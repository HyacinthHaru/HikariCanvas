# 0.7.1-P4 幽灵拖动设目标坐标 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在积木编辑器右侧预览框里，给"移到 / 改大小 / 旋转"坐标积木叠一个半透明元素虚影 + 控制点，让用户直接拖虚影设定目标 x/y/w/h/rotation，松手写回积木 patch。

**Architecture:** 纯前端（E3，后端零改）。两块纯逻辑抽成可单测模块——`clientToWall`（M1 坐标反算，以 canvas `getBoundingClientRect` 为原点消 round 偏差）+ `ghostDrag.ts`（虚影元素构造 / 控制点几何 / hit-test / 拖动写回算法）。`PreviewRenderer` 导出 `drawElement` 供虚影半透明渲染。`PreviewPane.vue` 叠虚影 + handle + 拖动交互（hit-test 优先虚影、落回 P3 点选）。`BlockNode.vue` 坐标字段 focusin → 设 `activeElementBinding`（聚焦坐标积木即显虚影，E12）。三批 P4a(移到)/P4b(改大小)/P4c(旋转) 各自独立实测。

**Tech Stack:** Vue 3 setup SFC + Pinia(scriptEdit store) + 原生 Canvas 2D + vitest（node 环境，无 jsdom）。

**契约:** `docs/scripting-0.7.1.md` §0(E10/E11/E12) + §2.3/§2.5。**实施前对照该文档。**

**测试基线（开工前）:** 后端 1703 / 前端 1086。本计划只动前端，后端数不变。

---

## 文件结构

**新建:**
- `web/src/script/canvas/ghostDrag.ts` — 虚影纯逻辑（kind 类型 / 元素构造 / 控制点位置 / hit-test / 拖动写回）。无 Vue / DOM 依赖，纯函数。
- `web/src/script/canvas/ghostDrag.test.ts` — ghostDrag vitest。
- `web/src/script/canvas/previewCoords.test.ts` — 若不存在则建；新增 `clientToWall` 的 round-trip / 偏差消除断言（已存在则追加）。

**修改:**
- `web/src/script/canvas/previewCoords.ts` — 加 `clientToWall`（M1 正解）。
- `web/src/render/PreviewRenderer.ts` — `export` 现有 `drawElement`（供虚影渲染复用）。
- `web/src/script/canvas/PreviewPane.vue` — 主战场：`activeCoordBlock` computed + 虚影渲染 + handle 渲染 + 拖动交互 + 坐标反算改 `clientToWall`。
- `web/src/script/canvas/BlockNode.vue` — 坐标 friendly 积木的参数字段 focusin → `setActiveElementBinding(path)`（E12）。
- `web/src/i18n/*` — P4c 收尾加 1~2 个提示 key（拖动手柄 tooltip）。

**不改:** scriptEdit store（复用现有 `activeElementBinding` + `updateActionField`，E12 决策"复用扩展"——`activeCoordBlock` 是 PreviewPane 的局部 computed，不进 store）。后端任何文件。

---

# 批次 P4a：移到（拖虚影中心设 x/y）—— 核心实测闸

P4a 含全部基建：坐标反算、虚影渲染、`activeCoordBlock` 派生、拖动框架、hit-test 优先级。P4b/P4c 在此之上加 handle。

---

### Task 1: `clientToWall` 坐标反算（M1 正解）

**Files:**
- Modify: `web/src/script/canvas/previewCoords.ts`
- Test: `web/src/script/canvas/previewCoords.test.ts`

- [ ] **Step 1: 写失败测试**

追加到 `previewCoords.test.ts`（若文件不存在则新建，顶部 `import { describe, it, expect } from 'vitest';` + `import { clientToWall } from './previewCoords';`）:

```typescript
describe('clientToWall (P4 M1 正解)', () => {
    // canvas CSS 宽是 round(wallW*scale)，本函数以 crect 真实尺寸比例映射，不经 transform.offset。
    const crect = { left: 100, top: 50, width: 256, height: 128 };
    const wallW = 512;
    const wallH = 256;

    it('canvas 左上角 client 点 → 墙原点 (0,0)', () => {
        const r = clientToWall(crect, wallW, wallH, 100, 50)!;
        expect(r.x).toBeCloseTo(0, 6);
        expect(r.y).toBeCloseTo(0, 6);
    });

    it('canvas 右下角 client 点 → 墙右下 (wallW, wallH)', () => {
        const r = clientToWall(crect, wallW, wallH, 100 + 256, 50 + 128)!;
        expect(r.x).toBeCloseTo(wallW, 6);
        expect(r.y).toBeCloseTo(wallH, 6);
    });

    it('中点 client → 墙中点', () => {
        const r = clientToWall(crect, wallW, wallH, 100 + 128, 50 + 64)!;
        expect(r.x).toBeCloseTo(wallW / 2, 6);
        expect(r.y).toBeCloseTo(wallH / 2, 6);
    });

    it('crect 任一维 ≤ 0 → null（canvas 未布局）', () => {
        expect(clientToWall({ left: 0, top: 0, width: 0, height: 128 }, wallW, wallH, 5, 5)).toBeNull();
        expect(clientToWall(crect, 0, wallH, 5, 5)).toBeNull();
    });
});
```

- [ ] **Step 2: 跑测试看红**

Run: `cd web && ./node_modules/.bin/vitest run src/script/canvas/previewCoords.test.ts`
Expected: FAIL — `clientToWall is not a function`。

- [ ] **Step 3: 实现 `clientToWall`**

在 `previewCoords.ts` 末尾追加:

```typescript
/**
 * 0.7.1-P4：指针 client 坐标 → 墙坐标，<b>以 canvas 真实 {@link DOMRect} 为原点比例映射</b>。
 *
 * <p>消除 P3 审查 M1：{@link computePreviewTransform} 的 offset/scale 用<b>未 round</b> 的尺寸算，
 * 但 canvas CSS 宽是 {@code round(wallW*scale)}，flex 居中下 canvas 真实左上角与 transform 原点
 * 差 ~0.5px。本函数不经 transform——直接用 {@code crect.width/height}（= round 后真实 CSS 尺寸）
 * 把 client 像素线性映回墙像素（canvas 内部分辨率 = 墙像素，1:1）。crect 任一维 ≤ 0 或墙未就绪
 * → null（canvas 尚未布局，调用方应早退不拖动）。</p>
 */
export function clientToWall(
    crect: { left: number; top: number; width: number; height: number },
    wallW: number,
    wallH: number,
    clientX: number,
    clientY: number,
): { x: number; y: number } | null {
    if (crect.width <= 0 || crect.height <= 0 || wallW <= 0 || wallH <= 0) return null;
    return {
        x: ((clientX - crect.left) * wallW) / crect.width,
        y: ((clientY - crect.top) * wallH) / crect.height,
    };
}
```

- [ ] **Step 4: 跑测试看绿**

Run: `cd web && ./node_modules/.bin/vitest run src/script/canvas/previewCoords.test.ts`
Expected: PASS（全部用例）。

- [ ] **Step 5: 提交**

```bash
git add web/src/script/canvas/previewCoords.ts web/src/script/canvas/previewCoords.test.ts
git commit -m "0.7.1-P4a: clientToWall 坐标反算(M1 正解,canvas rect 比例映射消 round 偏差)"
```

---

### Task 2: 导出 `drawElement` + 虚影元素构造 + 中心/旋转点几何

**Files:**
- Modify: `web/src/render/PreviewRenderer.ts`（仅给 `drawElement` 加 `export`）
- Create: `web/src/script/canvas/ghostDrag.ts`
- Test: `web/src/script/canvas/ghostDrag.test.ts`

- [ ] **Step 1: 写失败测试（构造 + 几何）**

新建 `ghostDrag.test.ts`:

```typescript
import { describe, it, expect } from 'vitest';
import {
    rotatePoint, normalizeDeg, buildGhostElement, ghostCenter,
} from './ghostDrag';
import type { Element } from '@/types/protocol';

function baseEl(over: Partial<Element> = {}): Element {
    return {
        id: 'e1', type: 'rect', x: 10, y: 20, w: 40, h: 60, rotation: 0,
        locked: false, visible: true, fill: { type: 'solid', color: '#fff' },
    } as unknown as Element;
}

describe('rotatePoint', () => {
    it('转 0 度 = 原点', () => {
        const p = rotatePoint(5, 7, 0, 0, 0);
        expect(p.x).toBeCloseTo(5, 6);
        expect(p.y).toBeCloseTo(7, 6);
    });
    it('绕原点顺时针 90 度(屏幕 y 向下):(1,0)→(0,1)', () => {
        const p = rotatePoint(1, 0, 0, 0, 90);
        expect(p.x).toBeCloseTo(0, 6);
        expect(p.y).toBeCloseTo(1, 6);
    });
    it('绕中心转后再反转回原点(round-trip)', () => {
        const f = rotatePoint(13, 29, 5, 5, 37);
        const b = rotatePoint(f.x, f.y, 5, 5, -37);
        expect(b.x).toBeCloseTo(13, 6);
        expect(b.y).toBeCloseTo(29, 6);
    });
});

describe('normalizeDeg', () => {
    it('负角 → [0,360)', () => expect(normalizeDeg(-90)).toBe(270));
    it('超 360 → 取模', () => expect(normalizeDeg(450)).toBe(90));
    it('四舍五入', () => expect(normalizeDeg(44.6)).toBe(45));
});

describe('buildGhostElement', () => {
    it('moveTo: patch x/y 覆盖,w/h/rotation 保留', () => {
        const g = buildGhostElement(baseEl(), 'moveTo', { x: '100', y: '200' });
        expect([g.x, g.y, g.w, g.h]).toEqual([100, 200, 40, 60]);
    });
    it('resize: patch w/h 覆盖(≥1),x/y 保留', () => {
        const g = buildGhostElement(baseEl(), 'resize', { w: '80', h: '0' });
        expect([g.x, g.y, g.w, g.h]).toEqual([10, 20, 80, 1]); // h=0 钳到 1
    });
    it('rotateTo: patch rotation 覆盖', () => {
        const g = buildGhostElement(baseEl(), 'rotateTo', { rotation: '45' });
        expect(g.rotation).toBe(45);
    });
    it('patch 缺/非数 → 退回原元素值', () => {
        const g = buildGhostElement(baseEl(), 'moveTo', { x: 'abc' });
        expect([g.x, g.y]).toEqual([10, 20]);
    });
});

describe('ghostCenter', () => {
    it('= (x+w/2, y+h/2)', () => {
        expect(ghostCenter(baseEl())).toEqual({ x: 30, y: 50 });
    });
});
```

- [ ] **Step 2: 跑测试看红**

Run: `cd web && ./node_modules/.bin/vitest run src/script/canvas/ghostDrag.test.ts`
Expected: FAIL — 模块/导出不存在。

- [ ] **Step 3a: 给 `drawElement` 加 export**

在 `web/src/render/PreviewRenderer.ts` 找到 `function drawElement(ctx: CanvasRenderingContext2D, e: Element, widthPx: number, heightPx: number): void {`（约 L103），把开头的 `function` 改成 `export function`。其余不动。

```typescript
// 之前: function drawElement(...)
export function drawElement(ctx: CanvasRenderingContext2D, e: Element, widthPx: number, heightPx: number): void {
```

> 说明：`drawElement` 内部已含 rotation（绕中心、度→弧度）+ opacity（`globalAlpha` 乘法）逻辑（PreviewRenderer.ts:103-125 已调研确认）。虚影渲染调用方包 `ctx.save()`+`globalAlpha=0.5`+`drawElement`+`restore()` 即得"半透明真样子"。

- [ ] **Step 3b: 建 `ghostDrag.ts`（本 Task 部分：类型 + 几何 + 构造）**

新建 `web/src/script/canvas/ghostDrag.ts`:

```typescript
/**
 * 0.7.1-P4：幽灵拖动纯逻辑（虚影元素构造 / 控制点几何 / hit-test / 拖动写回）。
 *
 * <p>无 Vue / DOM 依赖，纯函数，可单测。PreviewPane 用它把"绑定元素 + 积木 patch"转成虚影几何，
 * 把指针墙坐标转成新 patch（写回走 scriptEdit.updateActionField）。契约 docs/scripting-0.7.1.md
 * §2.3（E10/E11）。</p>
 */
import type { Element } from '@/types/protocol';

/** P4 支持幽灵拖动的坐标积木 kind（friendly setElementProperties 的 kind 子集）。 */
export type CoordKind = 'moveTo' | 'resize' | 'rotateTo';

/** 幽灵控制点种类：move=拖整块改 x/y；resizeSE=拖右下角改 w/h；rotate=转手柄改 rotation。 */
export type GhostHandle = 'move' | 'resizeSE' | 'rotate';

/** 旋转手柄距元素上沿的外延（墙像素）。 */
export const ROTATE_HANDLE_OFFSET = 24;

const RAD_PER_DEG = Math.PI / 180;

/** 是否 P4 支持拖动的坐标 kind。 */
export function isCoordKind(kind: string): kind is CoordKind {
    return kind === 'moveTo' || kind === 'resize' || kind === 'rotateTo';
}

/**
 * 点 (px,py) 绕中心 (cx,cy) 旋转 deg 度。屏幕坐标系（y 向下，正角顺时针）——与
 * PreviewRenderer.drawElement 的 {@code ctx.rotate(deg*PI/180)} 同向，保证虚影 handle 落在
 * 渲染出的虚影边角上。
 */
export function rotatePoint(px: number, py: number, cx: number, cy: number, deg: number): { x: number; y: number } {
    const rad = deg * RAD_PER_DEG;
    const cos = Math.cos(rad);
    const sin = Math.sin(rad);
    const dx = px - cx;
    const dy = py - cy;
    return { x: cx + dx * cos - dy * sin, y: cy + dx * sin + dy * cos };
}

/** 规范化角度到 [0,360) 整数。 */
export function normalizeDeg(deg: number): number {
    return ((Math.round(deg) % 360) + 360) % 360;
}

/** 解析 patch 数值，非有限 → fallback。 */
function num(s: string | undefined, fallback: number): number {
    if (s === undefined) return fallback;
    const v = Number(s);
    return Number.isFinite(v) ? v : fallback;
}

/**
 * 构造虚影元素：原元素副本 + patch 按 kind 覆盖目标几何。
 * moveTo→x/y；resize→w/h（钳 ≥1）；rotateTo→rotation。patch 缺 / 非数 → 退回原元素值。
 */
export function buildGhostElement(base: Element, kind: CoordKind, patch: Record<string, string>): Element {
    const g = { ...base } as Element;
    if (kind === 'moveTo') {
        g.x = num(patch.x, base.x);
        g.y = num(patch.y, base.y);
    } else if (kind === 'resize') {
        g.w = Math.max(1, num(patch.w, base.w));
        g.h = Math.max(1, num(patch.h, base.h));
    } else {
        g.rotation = num(patch.rotation, base.rotation);
    }
    return g;
}

/** 虚影中心（墙坐标，= 旋转中心）。 */
export function ghostCenter(g: Element): { x: number; y: number } {
    return { x: g.x + g.w / 2, y: g.y + g.h / 2 };
}
```

- [ ] **Step 4: 跑测试看绿**

Run: `cd web && ./node_modules/.bin/vitest run src/script/canvas/ghostDrag.test.ts`
Expected: PASS。

- [ ] **Step 5: 提交**

```bash
git add web/src/render/PreviewRenderer.ts web/src/script/canvas/ghostDrag.ts web/src/script/canvas/ghostDrag.test.ts
git commit -m "0.7.1-P4a: 导出 drawElement + ghostDrag 虚影构造/旋转点几何"
```

---

### Task 3: 控制点位置 + hit-test + 拖动写回（move 分支）

**Files:**
- Modify: `web/src/script/canvas/ghostDrag.ts`
- Test: `web/src/script/canvas/ghostDrag.test.ts`

> 本 Task 只把 move 路径打通（P4a 闸只需移到）；resizeSE/rotate 的 handle 位置与写回在 P4b/P4c 补。`ghostHandlePos` / `hitGhostHandle` / `applyGhostDrag` 三个函数本 Task 建出骨架并实现 move，resize/rotate 分支先返回空 / 占位，P4b/P4c 填。

- [ ] **Step 1: 写失败测试（move hit-test + 写回）**

追加到 `ghostDrag.test.ts`:

```typescript
import { ghostHandlePos, hitGhostHandle, applyGhostDrag } from './ghostDrag';

describe('hitGhostHandle — moveTo', () => {
    const g = buildGhostElement(baseEl(), 'moveTo', { x: '10', y: '20' }); // bbox [10,50]×[20,80]
    it('点落虚影 bbox 内 → move', () => {
        expect(hitGhostHandle(g, 'moveTo', 30, 50, 8)).toBe('move');
    });
    it('点落 bbox 外 → null', () => {
        expect(hitGhostHandle(g, 'moveTo', 5, 5, 8)).toBeNull();
    });
    it('旋转 90 度后,反旋判定仍正确(转后 bbox 在中心周围)', () => {
        const gr = { ...g, rotation: 90 };
        // 中心 (30,50) 一定命中
        expect(hitGhostHandle(gr, 'moveTo', 30, 50, 8)).toBe('move');
    });
});

describe('applyGhostDrag — move', () => {
    const g = buildGhostElement(baseEl(), 'moveTo', { x: '10', y: '20' });
    it('平移 delta 写回 x/y(round)', () => {
        const p = applyGhostDrag('moveTo', 'move', g, 30, 50, 130, 250); // 指针从中心移 (+100,+200)
        expect(p).toEqual({ x: '110', y: '220' });
    });
    it('负向平移', () => {
        const p = applyGhostDrag('moveTo', 'move', g, 30, 50, 20, 40);
        expect(p).toEqual({ x: '0', y: '10' });
    });
});

describe('ghostHandlePos — moveTo 无独立 handle', () => {
    it('moveTo 返回空对象(拖 bbox 整体,不画角点)', () => {
        expect(ghostHandlePos(baseEl(), 'moveTo')).toEqual({});
    });
});
```

- [ ] **Step 2: 跑测试看红**

Run: `cd web && ./node_modules/.bin/vitest run src/script/canvas/ghostDrag.test.ts`
Expected: FAIL — 三函数未定义。

- [ ] **Step 3: 实现 `ghostHandlePos` / `hitGhostHandle` / `applyGhostDrag`（move 完整，resize/rotate 占位）**

追加到 `ghostDrag.ts`:

```typescript
/**
 * 该 kind 要显示的独立控制点及其墙坐标位置（含 rotation）。
 * - moveTo：无独立 handle（拖虚影 bbox 整体），返空。
 * - resize：resizeSE = 右下角（随 rotation 转）。【P4b 实现】
 * - rotateTo：rotate = 中心正上方外延（随 rotation 转）。【P4c 实现】
 */
export function ghostHandlePos(
    g: Element,
    kind: CoordKind,
): Partial<Record<GhostHandle, { x: number; y: number }>> {
    const c = ghostCenter(g);
    const deg = g.rotation ?? 0;
    if (kind === 'resize') {
        return { resizeSE: rotatePoint(g.x + g.w, g.y + g.h, c.x, c.y, deg) };
    }
    if (kind === 'rotateTo') {
        return { rotate: rotatePoint(c.x, g.y - ROTATE_HANDLE_OFFSET, c.x, c.y, deg) };
    }
    return {};
}

/**
 * hit-test：墙坐标点命中哪个 handle（半径 rWall 墙像素）。先判专属 handle（距离 ≤ rWall），
 * moveTo 判点是否落在虚影 bbox（含 rotation：反旋指针回未转空间判矩形）。无命中 → null。
 */
export function hitGhostHandle(
    g: Element,
    kind: CoordKind,
    wx: number,
    wy: number,
    rWall: number,
): GhostHandle | null {
    const handles = ghostHandlePos(g, kind);
    for (const key of Object.keys(handles) as GhostHandle[]) {
        const p = handles[key];
        if (p && Math.hypot(wx - p.x, wy - p.y) <= rWall) return key;
    }
    if (kind === 'moveTo') {
        const c = ghostCenter(g);
        const local = rotatePoint(wx, wy, c.x, c.y, -(g.rotation ?? 0));
        if (local.x >= g.x && local.x <= g.x + g.w && local.y >= g.y && local.y <= g.y + g.h) {
            return 'move';
        }
    }
    return null;
}

/**
 * 拖动写回：给定 handle + 拖起虚影 startG + 拖起指针墙坐标 + 当前指针墙坐标 → 新 patch（String 化）。
 * - move：平移 delta（startG.x + dx，round）。
 * - resizeSE：左上锚定，新 w/h = 指针到左上的距离（反旋到未转空间，≥1）。【P4b】
 * - rotate：中心→指针角度 + 90°（手柄正上方时 rotation=0），normalize。【P4c】
 * 未实现的分支返回空对象（不写回）。
 */
export function applyGhostDrag(
    kind: CoordKind,
    handle: GhostHandle,
    startG: Element,
    startWx: number,
    startWy: number,
    curWx: number,
    curWy: number,
): Record<string, string> {
    if (handle === 'move') {
        const dx = curWx - startWx;
        const dy = curWy - startWy;
        return { x: String(Math.round(startG.x + dx)), y: String(Math.round(startG.y + dy)) };
    }
    void kind;
    return {};
}
```

- [ ] **Step 4: 跑测试看绿**

Run: `cd web && ./node_modules/.bin/vitest run src/script/canvas/ghostDrag.test.ts`
Expected: PASS。

- [ ] **Step 5: 提交**

```bash
git add web/src/script/canvas/ghostDrag.ts web/src/script/canvas/ghostDrag.test.ts
git commit -m "0.7.1-P4a: ghostDrag handle 位置/hit-test/move 写回(resize/rotate 占位)"
```

---

### Task 4: PreviewPane 派生 `activeCoordBlock` + 渲染虚影

**Files:**
- Modify: `web/src/script/canvas/PreviewPane.vue`

> 无新单测（Vue SFC + canvas 渲染，项目惯例靠纯逻辑单测 + 用户实测；PreviewPane 现有逻辑也无 SFC 单测）。本 Task 让"聚焦坐标积木 → 预览叠半透明虚影"，先不可拖（Task 5 加交互）。

- [ ] **Step 1: import + activeCoordBlock computed**

在 `PreviewPane.vue` `<script setup>` 顶部 import 区加:

```typescript
import { computed } from 'vue'; // 若已 import 则合并
import { drawElement } from '@/render/PreviewRenderer';
import {
    isCoordKind, buildGhostElement, ghostHandlePos,
    type CoordKind, type GhostHandle,
} from './ghostDrag';
import { clientToWall } from './previewCoords';
```

在现有 `boundElementId()` 附近新增（派生当前坐标积木——E12 复用 `activeElementBinding`）:

```typescript
/**
 * 0.7.1-P4：当前"坐标积木"（activeElementBinding 指向的 action 若是 setElementProperties +
 * kind∈{moveTo,resize,rotateTo} + 已绑可见元素）。null = 不显虚影。聚焦坐标积木的任意字段
 * （BlockNode focusin）→ activeElementBinding=path → 此 computed 命中 → 叠虚影。
 */
const activeCoordBlock = computed<
    { path: string; kind: CoordKind; baseEl: Element; patch: Record<string, string> } | null
>(() => {
    const path = edit.activeElementBinding;
    const rule = edit.workingCopy;
    if (!path || !rule) return null;
    const action = getAt(rule.actions, parsePath(path));
    if (!action || action.type !== 'setElementProperties') return null;
    const kind = (action as { kind?: string }).kind ?? '';
    if (!isCoordKind(kind)) return null;
    const elementId = (action as { elementId?: string }).elementId;
    if (typeof elementId !== 'string' || !elementId) return null;
    const baseEl = visibleElements().find((e) => e.id === elementId);
    if (!baseEl) return null;
    const patch = (action as { patch?: Record<string, string> }).patch ?? {};
    return { path, kind, baseEl, patch };
});

/** 当前虚影元素（绑定元素 + patch 覆盖目标几何）；无坐标积木 → null。 */
const ghostEl = computed<Element | null>(() => {
    const cb = activeCoordBlock.value;
    return cb ? buildGhostElement(cb.baseEl, cb.kind, cb.patch) : null;
});
```

- [ ] **Step 2: paint() 里叠画虚影**

在 `paint()` 末尾、`drawBindingHighlight(...)` 之后追加虚影渲染:

```typescript
    // P4：当前坐标积木的半透明虚影（叠在墙渲染 + 绑定高亮之上）。
    drawGhost(ctx, transform.value.scale);
```

新增函数（紧邻 `drawBindingHighlight`）:

```typescript
/**
 * P4：画当前坐标积木的虚影（元素半透明真样子，E10）+ 控制点 handle。ctx 处于墙像素空间（1:1），
 * 直接用墙坐标。半透明 = save + globalAlpha=0.5 + drawElement + restore。handle 半径按 scale
 * 反补偿（显示恒定）。
 */
function drawGhost(ctx: CanvasRenderingContext2D, scale: number): void {
    const cb = activeCoordBlock.value;
    const g = ghostEl.value;
    if (!cb || !g) return;
    const wallW = project.canvasPixelWidth;
    const wallH = project.canvasPixelHeight;
    // 半透明虚影本体。
    ctx.save();
    ctx.globalAlpha = 0.5;
    drawElement(ctx, g, wallW, wallH);
    ctx.restore();
    // 虚影轮廓（mauve 实线，区别于绑定高亮的描边）+ handle。
    if (typeof ctx.strokeRect !== 'function') return;
    const lw = scale > 0 ? Math.max(1, 1.5 / scale) : 1.5;
    const rWall = scale > 0 ? GHOST_HANDLE_RADIUS_CSS / scale : GHOST_HANDLE_RADIUS_CSS;
    ctx.save();
    ctx.strokeStyle = '#cba6f7';
    ctx.lineWidth = lw;
    if (typeof ctx.setLineDash === 'function') ctx.setLineDash([]);
    drawGhostOutline(ctx, g);
    // handle（resize/rotate；moveTo 无独立 handle）。
    const handles = ghostHandlePos(g, cb.kind);
    ctx.fillStyle = '#cba6f7';
    for (const key of Object.keys(handles) as GhostHandle[]) {
        const p = handles[key];
        if (!p || typeof ctx.arc !== 'function') continue;
        ctx.beginPath();
        ctx.arc(p.x, p.y, rWall, 0, Math.PI * 2);
        ctx.fill();
    }
    ctx.restore();
}

/** 画虚影 bbox 轮廓（含 rotation：4 角连线）。 */
function drawGhostOutline(ctx: CanvasRenderingContext2D, g: Element): void {
    const cx = g.x + g.w / 2;
    const cy = g.y + g.h / 2;
    const deg = g.rotation ?? 0;
    const corners = [
        [g.x, g.y], [g.x + g.w, g.y], [g.x + g.w, g.y + g.h], [g.x, g.y + g.h],
    ].map(([px, py]) => rotatePointLocal(px, py, cx, cy, deg));
    if (typeof ctx.beginPath !== 'function') return;
    ctx.beginPath();
    ctx.moveTo(corners[0].x, corners[0].y);
    for (let i = 1; i < corners.length; i++) ctx.lineTo(corners[i].x, corners[i].y);
    ctx.closePath();
    ctx.stroke();
}
```

在 import 区加 `rotatePoint`（重命名本地别名避免与潜在符号冲突）+ 常量:

```typescript
import { rotatePoint as rotatePointLocal } from './ghostDrag';
// 文件内常量（与 BlockNode 无关）：
const GHOST_HANDLE_RADIUS_CSS = 7; // handle 显示半径(CSS px)，rWall = 此值/scale
```

> 合并提示：Task 4 Step 1 已 import 了 ghostDrag 的若干符号，这里把 `rotatePoint as rotatePointLocal` 合进同一条 import。

- [ ] **Step 3: watch 触发虚影重绘**

在现有 `watch(() => boundElementId(), ...)` 附近追加（patch / 绑定变 → 重画虚影）:

```typescript
// P4：坐标积木 patch / 虚影几何变化 → 重绘虚影。
watch(() => ghostEl.value, () => requestPaint(), { deep: true });
```

- [ ] **Step 4: 类型检查 + build**

Run: `cd web && ./node_modules/.bin/vite build --clearScreen false`
Expected: build 成功（rolldown 即类型/构建闸；本项目 vue-tsc 在 Node25 坏，build 是权威）。若报 `Element` 未 import，在 import 区确认 `import type { Element } from '@/types/protocol';` 已存在（PreviewPane 现有已 import）。

- [ ] **Step 5: 提交**

```bash
git add web/src/script/canvas/PreviewPane.vue
git commit -m "0.7.1-P4a: PreviewPane 派生 activeCoordBlock + 叠半透明虚影渲染"
```

---

### Task 5: BlockNode 坐标字段 focusin → 显虚影（E12）

**Files:**
- Modify: `web/src/script/canvas/BlockNode.vue`

- [ ] **Step 1: 找到 friendly 参数字段渲染处**

`BlockNode.vue` 模板里 `v-for="f in friendlyFields"` 渲染坐标输入（x/y/w/h/rotation）。现有元素字段用 `<span @focusin="onElementFieldFocus">` 设 `activeElementBinding`（PreviewPane.vue 调研已见 onElementFieldFocus → setActiveElementBinding(path)）。本 Task 让坐标字段 focusin 也设同一 path——聚焦坐标积木任意字段即显虚影。

- [ ] **Step 2: 给坐标字段容器加 focusin**

把 friendlyFields 的 `v-for` `BlockParamInput` 外层包一个 `<span @focusin="onFriendlyFieldFocus">`（或在其所在容器加），其中 `onFriendlyFieldFocus` 复用现有绑定 setter:

```vue
<!-- 皮肤字段（moveTo/resize/rotateTo 的 x/y/w/h/rotation 等）：focusin → 显虚影(E12) -->
<span
    v-for="f in friendlyFields"
    :key="f.name"
    class="hc-block-param-input"
    @focusin="onFriendlyFieldFocus"
>
    <BlockParamInput
        :field="f"
        :value="friendlyFieldValue(f)"
        :action-kind="action.type"
        :disabled="locked"
        @update="(v: unknown) => onFriendlyFieldUpdate(f, v)"
    />
</span>
```

`<script setup>` 加（紧邻现有 `onElementFieldFocus`）:

```typescript
/**
 * 0.7.1-P4（E12）：聚焦坐标积木的参数字段（x/y/w/h/rotation）→ 设 activeElementBinding 为本
 * 积木 path，让 PreviewPane 自动显该积木虚影可拖。与元素字段 focusin 同 setter（同一 path）。
 */
function onFriendlyFieldFocus(): void {
    edit.setActiveElementBinding(props.path);
}
```

> 若现有 `friendlyFields` 的 v-for 直接在 `BlockParamInput` 上而非 span，按上面结构改成 span 包裹（保留原 `class="hc-block-param-input"` 与 `:key`）。`onElementFieldFocus`/`edit` 已在 script 中存在（P3 引入），复用即可。

- [ ] **Step 3: build 验证**

Run: `cd web && ./node_modules/.bin/vite build --clearScreen false`
Expected: 成功。

- [ ] **Step 4: 提交**

```bash
git add web/src/script/canvas/BlockNode.vue
git commit -m "0.7.1-P4a: 坐标积木字段 focusin → 显虚影(E12 聚焦即显)"
```

---

### Task 6: PreviewPane 虚影拖动交互（move）+ hit-test 优先级

**Files:**
- Modify: `web/src/script/canvas/PreviewPane.vue`

- [ ] **Step 1: 拖动状态 + 工具函数**

`<script setup>` 加拖动局部状态（紧邻 transform ref）:

```typescript
/** P4：当前虚影拖动会话；null = 未拖动。startG = 拖起时的虚影元素快照（写回基准）。 */
interface GhostDragSession {
    handle: GhostHandle;
    path: string;
    kind: CoordKind;
    startG: Element;
    startWx: number;
    startWy: number;
}
let ghostDrag: GhostDragSession | null = null;

/** client → 墙坐标，以 canvas 真实 rect 为原点（M1 正解，绕开 transform.offset）。 */
function clientToWallCoord(clientX: number, clientY: number): { x: number; y: number } | null {
    const canvas = canvasRef.value;
    if (!canvas || typeof canvas.getBoundingClientRect !== 'function') return null;
    const crect = canvas.getBoundingClientRect();
    return clientToWall(crect, project.canvasPixelWidth, project.canvasPixelHeight, clientX, clientY);
}
```

- [ ] **Step 2: onPointerDown 加虚影 hit-test 优先级**

改现有 `onPointerDown`：先判虚影 handle（命中 → 起拖，绑 window 监听，**不**走点选）；否则落回 P3 元素点选。把现有 P3 点选体保留为"无虚影命中"分支:

```typescript
function onPointerDown(e: PointerEvent): void {
    if (e.button !== undefined && e.button !== 0) return;
    const wallW = project.canvasPixelWidth;
    const wallH = project.canvasPixelHeight;
    if (!project.state || wallW <= 0 || wallH <= 0) return;

    // P4：先判虚影控制点（hit-test 优先于 P3 点选）。
    const cb = activeCoordBlock.value;
    const g = ghostEl.value;
    if (cb && g) {
        const w = clientToWallCoord(e.clientX, e.clientY);
        if (w) {
            const scale = transform.value.scale;
            const rWall = scale > 0 ? GHOST_HANDLE_RADIUS_CSS / scale : GHOST_HANDLE_RADIUS_CSS;
            const handle = hitGhostHandle(g, cb.kind, w.x, w.y, rWall);
            if (handle) {
                ghostDrag = {
                    handle, path: cb.path, kind: cb.kind,
                    startG: g, startWx: w.x, startWy: w.y,
                };
                window.addEventListener('pointermove', onGhostPointerMove);
                window.addEventListener('pointerup', onGhostPointerUp);
                window.addEventListener('pointercancel', onGhostPointerUp);
                e.preventDefault();
                return; // 进拖动，不走点选
            }
        }
    }

    // P3：落回元素点选（改绑 elementId）。
    const w = clientToWallCoord(e.clientX, e.clientY);
    if (!w) return;
    const el = findElementAt(w.x, w.y);
    if (!el) return;
    applyPickedElement(el);
}
```

> 注意：`onPointerDown` 内 P3 点选段改用 `clientToWallCoord`（M1 正解），替换原来的 `host.getBoundingClientRect()` + `previewToWall`。原 `import { previewToWall }` 若不再用可删（保留 `computePreviewTransform`）。

- [ ] **Step 3: 拖动 move/up（window 监听，照 splitter 范式防 capture 丢失）**

新增（hitGhostHandle / applyGhostDrag 已 import）:

```typescript
/** P4：虚影拖动中。算新 patch（本地 mutate workingCopy 让虚影跟手），不在此发保存（debounce 自管）。 */
function onGhostPointerMove(e: PointerEvent): void {
    if (!ghostDrag) return;
    const w = clientToWallCoord(e.clientX, e.clientY);
    if (!w) return;
    const patch = applyGhostDrag(
        ghostDrag.kind, ghostDrag.handle, ghostDrag.startG,
        ghostDrag.startWx, ghostDrag.startWy, w.x, w.y,
    );
    if (Object.keys(patch).length === 0) return;
    // 合并进现有 patch（保留其它键），走唯一写回入口；updateActionField 内已 pushUndo + debounce save。
    const cb = activeCoordBlock.value;
    const basePatch = cb?.patch ?? {};
    edit.updateActionField(ghostDrag.path, {
        patch: { ...basePatch, ...patch },
    } as Partial<ScriptAction>);
}

/** P4：松手——解绑 window 监听，结束拖动会话。写回已在 move 里逐帧落（debounce save 收口）。 */
function onGhostPointerUp(): void {
    ghostDrag = null;
    window.removeEventListener('pointermove', onGhostPointerMove);
    window.removeEventListener('pointerup', onGhostPointerUp);
    window.removeEventListener('pointercancel', onGhostPointerUp);
}
```

> `startG` 是拖起时快照——`applyGhostDrag('move')` 用 `startG.x + (cur−start)` 算，逐帧基于快照不累积漂移。`updateActionField` 每帧 pushUndo 会塞 undo 栈，但 P3 既有参数输入同样每次 update 都 pushUndo，行为一致（cap 50 自然滚动）；如实测嫌 undo 噪音，P4c 收尾再做"拖动期合并 undo"优化（非闸）。

- [ ] **Step 4: onBeforeUnmount 清理拖动监听**

在现有 `onBeforeUnmount` 里追加:

```typescript
    // P4：拖动中卸载 → 解绑 window 监听防泄漏。
    if (ghostDrag) onGhostPointerUp();
```

- [ ] **Step 5: 光标提示 + build**

模板 canvas 的 `:class` 加虚影可拖光标（有坐标积木时 `cursor: move`），与 `hc-preview-canvas-pickable` 并存:

```vue
:class="{
    'hc-preview-canvas-pickable': edit.activeElementBinding !== null,
    'hc-preview-canvas-ghost': activeCoordBlock !== null,
}"
```

`<style scoped>` 加:

```css
.hc-preview-canvas-ghost { cursor: move; }
```

Run: `cd web && ./node_modules/.bin/vite build --clearScreen false`
Expected: 成功。

- [ ] **Step 6: 全量前端测试 + 提交**

```bash
cd web && pkill -f vitest; ./node_modules/.bin/vitest run
```
Expected: 全绿（基线 1086 + Task1/2/3 新增用例）。

```bash
git add web/src/script/canvas/PreviewPane.vue
git commit -m "0.7.1-P4a: 虚影拖动交互(move) + hit-test 优先级 + M1 坐标反算落地"
```

---

**P4a 闸（核心实测，打包后验）：** 聚焦"移到"积木 + 绑元素 → 预览出现半透明虚影；拖虚影到目标位置 → 积木 x/y 跟着变；部署墙触发该脚本 → 元素真移到那。**这是 0.7.1 的灵魂功能。**

---

# 批次 P4b：改大小（拖右下角 handle 设 w/h）

### Task 7: applyGhostDrag resize 分支

**Files:**
- Modify: `web/src/script/canvas/ghostDrag.ts`
- Test: `web/src/script/canvas/ghostDrag.test.ts`

- [ ] **Step 1: 写失败测试**

追加到 `ghostDrag.test.ts`:

```typescript
describe('ghostHandlePos — resize', () => {
    it('未旋转: resizeSE = 右下角', () => {
        const g = buildGhostElement(baseEl(), 'resize', { w: '40', h: '60' }); // x10 y20 w40 h60
        expect(ghostHandlePos(g, 'resize').resizeSE).toEqual({ x: 50, y: 80 });
    });
});

describe('hitGhostHandle — resize', () => {
    const g = buildGhostElement(baseEl(), 'resize', { w: '40', h: '60' });
    it('点近右下角 → resizeSE', () => {
        expect(hitGhostHandle(g, 'resize', 50, 80, 8)).toBe('resizeSE');
    });
    it('点远离 handle → null（resize 无 move 整体拖）', () => {
        expect(hitGhostHandle(g, 'resize', 30, 50, 8)).toBeNull();
    });
});

describe('applyGhostDrag — resizeSE', () => {
    const g = buildGhostElement(baseEl(), 'resize', { w: '40', h: '60' }); // 左上 (10,20)
    it('拖右下角到 (110,220) → w=100 h=200(左上锚定)', () => {
        const p = applyGhostDrag('resize', 'resizeSE', g, 50, 80, 110, 220);
        expect(p).toEqual({ w: '100', h: '200' });
    });
    it('拖到左上内侧 → w/h 钳 ≥1', () => {
        const p = applyGhostDrag('resize', 'resizeSE', g, 50, 80, 5, 5);
        expect(p).toEqual({ w: '1', h: '1' });
    });
});
```

- [ ] **Step 2: 跑测试看红**

Run: `cd web && ./node_modules/.bin/vitest run src/script/canvas/ghostDrag.test.ts`
Expected: FAIL — `applyGhostDrag` resize 返空对象。

- [ ] **Step 3: 实现 resize 分支**

`ghostDrag.ts` 的 `applyGhostDrag` 里，把 `void kind; return {};` 之前插入 resizeSE 分支:

```typescript
    if (handle === 'resizeSE') {
        // 反旋指针到未转空间（绕拖起虚影中心），右下角 = 指针，左上 (x,y) 锚定。
        const c = ghostCenter(startG);
        const local = rotatePoint(curWx, curWy, c.x, c.y, -(startG.rotation ?? 0));
        const w = Math.max(1, Math.round(local.x - startG.x));
        const h = Math.max(1, Math.round(local.y - startG.y));
        return { w: String(w), h: String(h) };
    }
```

- [ ] **Step 4: 跑测试看绿**

Run: `cd web && ./node_modules/.bin/vitest run src/script/canvas/ghostDrag.test.ts`
Expected: PASS。

- [ ] **Step 5: 提交**

```bash
git add web/src/script/canvas/ghostDrag.ts web/src/script/canvas/ghostDrag.test.ts
git commit -m "0.7.1-P4b: ghostDrag resize 写回(右下角 handle,左上锚定,w/h≥1)"
```

---

### Task 8: PreviewPane resize handle 渲染（已通用，验证）

**Files:**
- Modify: `web/src/script/canvas/PreviewPane.vue`（多数已在 Task 4/6 通用代码覆盖）

- [ ] **Step 1: 确认 resize handle 已渲染 + 可拖**

Task 4 的 `drawGhost` 已按 `ghostHandlePos(g, cb.kind)` 通用画 handle（resize 返 resizeSE 圆点）；Task 6 的 `onPointerDown` 已通用 `hitGhostHandle`（resize 命中 resizeSE 起拖）+ `onGhostPointerMove` 已通用 `applyGhostDrag`。故 P4b 在 ghostDrag resize 分支（Task 7）补齐后**自动生效**，PreviewPane 无需新代码。

- [ ] **Step 2: build 验证**

Run: `cd web && ./node_modules/.bin/vite build --clearScreen false`
Expected: 成功。

- [ ] **Step 3: （若 build 暴露 resize 专属问题才改）** 否则跳过，无提交。

> 此 Task 多为验证。若实测发现 resize 虚影需要"四角都能拖"，留 P4c 收尾或 v1.x（v1 单 SE 角够用）。

---

# 批次 P4c：旋转（转手柄设 rotation）

### Task 9: applyGhostDrag rotate 分支

**Files:**
- Modify: `web/src/script/canvas/ghostDrag.ts`
- Test: `web/src/script/canvas/ghostDrag.test.ts`

- [ ] **Step 1: 写失败测试**

追加到 `ghostDrag.test.ts`:

```typescript
describe('ghostHandlePos — rotateTo', () => {
    it('未旋转: rotate handle 在中心正上方外延', () => {
        const g = buildGhostElement(baseEl(), 'rotateTo', { rotation: '0' }); // 中心 (30,50)
        const p = ghostHandlePos(g, 'rotateTo').rotate!;
        expect(p.x).toBeCloseTo(30, 6);
        expect(p.y).toBeCloseTo(20 - 24, 6); // g.y(20) - ROTATE_HANDLE_OFFSET(24)
    });
});

describe('applyGhostDrag — rotate', () => {
    const g = buildGhostElement(baseEl(), 'rotateTo', { rotation: '0' }); // 中心 (30,50)
    it('指针在正上方 → rotation 0', () => {
        const p = applyGhostDrag('rotateTo', 'rotate', g, 30, 0, 30, -10);
        expect(p).toEqual({ rotation: '0' });
    });
    it('指针在正右方 → rotation 90', () => {
        const p = applyGhostDrag('rotateTo', 'rotate', g, 30, 0, 60, 50);
        expect(p).toEqual({ rotation: '90' });
    });
    it('指针在正下方 → rotation 180', () => {
        const p = applyGhostDrag('rotateTo', 'rotate', g, 30, 0, 30, 100);
        expect(p).toEqual({ rotation: '180' });
    });
    it('指针在正左方 → rotation 270', () => {
        const p = applyGhostDrag('rotateTo', 'rotate', g, 30, 0, 0, 50);
        expect(p).toEqual({ rotation: '270' });
    });
});
```

- [ ] **Step 2: 跑测试看红**

Run: `cd web && ./node_modules/.bin/vitest run src/script/canvas/ghostDrag.test.ts`
Expected: FAIL — rotate 返空对象。

- [ ] **Step 3: 实现 rotate 分支**

`applyGhostDrag` 里 resizeSE 分支后、`void kind; return {};` 前插入:

```typescript
    if (handle === 'rotate') {
        // 中心→指针的角度 + 90°：手柄正上方（atan2=-90°）时 rotation=0；正右(0°)→90°，与渲染顺时针一致。
        const c = ghostCenter(startG);
        const deg = normalizeDeg((Math.atan2(curWy - c.y, curWx - c.x) * 180) / Math.PI + 90);
        return { rotation: String(deg) };
    }
```

- [ ] **Step 4: 跑测试看绿**

Run: `cd web && ./node_modules/.bin/vitest run src/script/canvas/ghostDrag.test.ts`
Expected: PASS（含正上/右/下/左四向）。

- [ ] **Step 5: 提交**

```bash
git add web/src/script/canvas/ghostDrag.ts web/src/script/canvas/ghostDrag.test.ts
git commit -m "0.7.1-P4c: ghostDrag rotate 写回(中心→指针角度+90°,normalize)"
```

---

### Task 10: 旋转手柄渲染连杆 + 收尾

**Files:**
- Modify: `web/src/script/canvas/PreviewPane.vue`
- Modify: i18n（`web/src/i18n/` 中英）

- [ ] **Step 1: 旋转手柄加连杆（中心到手柄一条线，视觉提示可转）**

`drawGhost` 里画 handle 的循环前，对 rotate handle 加连杆。在 `const handles = ghostHandlePos(...)` 后:

```typescript
    // rotate handle：画一条中心→手柄的连杆（视觉提示"转")。
    if (cb.kind === 'rotateTo' && handles.rotate && typeof ctx.moveTo === 'function') {
        const c = { x: g.x + g.w / 2, y: g.y + g.h / 2 };
        ctx.beginPath();
        ctx.moveTo(c.x, c.y);
        ctx.lineTo(handles.rotate.x, handles.rotate.y);
        ctx.stroke();
    }
```

- [ ] **Step 2: i18n 提示 key（拖动手柄 tooltip / 空绑提示）**

`web/src/i18n/` 中英各加（找到 `script.preview` 段，键名按现有风格）:

```typescript
// zh
preview: {
    // ...现有...
    ghostHint: '拖虚影设目标：移到拖中心、改大小拖角、旋转转手柄',
}
// en
preview: {
    // ...
    ghostHint: 'Drag the ghost to set target: move = center, resize = corner, rotate = handle',
}
```

在 PreviewPane 模板预览框标题旁或 host 角落加一行小字提示（仅 `activeCoordBlock !== null` 时显）:

```vue
<div v-if="activeCoordBlock" class="hc-preview-ghost-hint">{{ t.script.preview.ghostHint }}</div>
```

```css
.hc-preview-ghost-hint {
    position: absolute; bottom: 0.25rem; left: 0.25rem; right: 0.25rem;
    padding: 0.2rem 0.4rem; font-size: 0.65rem; text-align: center;
    color: var(--muted-foreground);
    background: color-mix(in srgb, var(--card) 80%, transparent);
    border-radius: var(--radius-sm); pointer-events: none;
}
```

- [ ] **Step 3: 全量测试 + build**

```bash
cd web && pkill -f vitest; ./node_modules/.bin/vitest run
```
Expected: 全绿。

Run: `cd web && ./node_modules/.bin/vite build --clearScreen false`
Expected: 成功。

- [ ] **Step 4: 提交**

```bash
git add web/src/script/canvas/PreviewPane.vue web/src/i18n
git commit -m "0.7.1-P4c: 旋转手柄连杆 + 幽灵拖动提示文案 i18n"
```

---

### Task 11: 打包 + journal + 版本日志

**Files:**
- Modify: `docs/journal.md`（顶部追加）

- [ ] **Step 1: 重新打包供用户实测**

```bash
cd web && ./node_modules/.bin/vite build --clearScreen false
cd /Users/haru/Desktop/项目/HikariCanvas && ./gradlew :plugin:shadowJar
```
Expected: `web/dist` 更新 + `HikariCanvas-0.7.1-SNAPSHOT.jar` 产出。前端产物经 processResources 进 jar。

- [ ] **Step 2: journal 追加**

`docs/journal.md` 顶部加一条（日期 2026-06-13 · 范围 0.7.1-P4 · 改动 · 关联文件），照现有条目格式。

- [ ] **Step 3: 提交 + push**

```bash
git add docs/journal.md
git commit -m "0.7.1-P4: 幽灵拖动设目标坐标(移到/改大小/旋转)完工 + journal"
git push origin main
```

> push 后用 `gh api /repos/HyacinthHaru/HikariCanvas/commits/<sha> --jq '.commit.verification.verified'` 抽查签名（应 true）。

---

## P4 完整实测闸（打包后用户验）

1. **移到（P4a 核心）**：编辑器拖个文本/图形元素 → 新建脚本规则 → 加"移动元素到 (x,y)"积木 → 用"从预览点选"准星点中该元素（绑定）→ 聚焦积木的 x/y 字段 → 预览出现半透明虚影 → 拖虚影到目标 → 积木 x/y 跟着变 → 部署墙、触发脚本 → 元素真移到目标。
2. **改大小（P4b）**：加"改变元素大小 (w,h)"积木 + 绑元素 → 聚焦字段 → 虚影右下角出现圆点 → 拖角 → w/h 变 → 触发后元素真变大/小。
3. **旋转（P4c）**：加"旋转元素到 N°"积木 + 绑元素 → 虚影上方出现手柄 + 连杆 → 转手柄 → rotation 变 → 触发后元素真转。
4. **E5 单虚影**：脚本里多个坐标积木 → 切聚焦不同积木 → 预览只显当前那个的虚影。
5. **M1 精度**：缩小预览框（拖窄分栏）使 scale<1 → 拖虚影到某像素 → 写回坐标与目视一致，无 ~1px 偏移。
6. **不抢点选**：聚焦坐标积木时，点虚影外的别的元素 → 仍能改绑（P3 点选不被虚影吃）。

---

## Self-Review（写完计划自查）

- **Spec 覆盖**：E10 虚影真样子(Task 2/4 drawElement+drawGhost) ✅；E11 三 transform(P4a move / P4b resize / P4c rotate) ✅；E12 聚焦即显(Task 5 BlockNode focusin + Task 4 activeCoordBlock) ✅；M1 正解(Task 1 clientToWall + Task 6 落地) ✅；§2.3 hit-test 优先级(Task 6) ✅；§2.5 写回走 updateActionField(Task 6) ✅。
- **类型一致**：`CoordKind`/`GhostHandle`/`buildGhostElement`/`ghostHandlePos`/`hitGhostHandle`/`applyGhostDrag`/`clientToWall`/`rotatePoint`/`normalizeDeg`/`ghostCenter` 跨 Task 签名一致；PreviewPane 用 `rotatePoint as rotatePointLocal`、`GHOST_HANDLE_RADIUS_CSS` 常量一致。
- **占位扫描**：无 TODO/TBD；resize/rotate 分支"占位"是有意分批（Task 3 明确返空 + Task 7/9 填实），非 placeholder。
- **无后端改动**：全部文件在 `web/`，符合 E3 纯前端。
- **风险点**：旋转态 resize 用拖起中心反旋是近似（rotation≠0 时 center 随 w/h 漂移），v1 接受（常见 resize 在 rotation=0）；undo 噪音（拖动逐帧 pushUndo）与 P3 既有行为一致，留实测。
