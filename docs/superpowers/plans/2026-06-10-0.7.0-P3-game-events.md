# 0.7.0-P3 游戏事件层 + 命令模板 + 试跑轨迹 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development。2 批次,每批一轮合并审查(规格+质量)。

**Goal:** 补齐 6 触发器(进服/击杀/玩家靠近)+ 第 8 动作真实化(runCommand 命令模板)+ script.test 异步试跑轨迹 + 条件保存期预 parse。P3 闸:6 触发器 8 动作全通(单测)。

**执行纪律:** 同 P1/P2。基线:后端 1515 / 前端 529。

## 关键决策(锁死)

| # | 决策 | 理由 |
|---|---|---|
| K11 | **script.test 异步化**:ack 立即返 `{accepted:true, ruleId}`;轨迹完成后走 **S→C 推送 op `script.trace {ruleId, steps}`**(推给本墙 session,照 patch 推送通道) | P1 终审账单:同步 seam 阻塞 Jetty worker,合法规则可串 wait 至分钟级,5s ack 超时必爆 |
| K12 | **TEST run 不豁免 Budget**(runs/s/actions/chain 全过)+ TriggerContext.Source.TEST 标记 audit | 试跑即真实执行(D5),手动点击 10/s 足够 |
| K13 | **命令模板参数转义**:`{param}` 替换值剥换行/`§`;含 `@` 整体拒(error step)除非 ParamSpec `type: online-player`(此时值必须命中在线玩家名);默认 max-length 64;模板查无 → blocked step(同 P2 形态) | scripting.md §5.2 |
| K14 | **playerNear 采样器**:Bukkit 主线程定时 task,间隔 config `scripts.player-near-sample-ticks` 默 10;按世界分桶 + 距离平方;**进入沿触发**(per player×rule 内/外状态机,离开重置);玩家退出清状态;触发投递 TriggerContext(detail=玩家名) | scripting.md §2.2 |
| K15 | **join/kill 全局触发**:Router 加全局索引 `Set<RuleRef> byGlobalTrigger[playerJoin\|playerKill]`(rebuild 时维护);事件 → 遍历(规则量受配额约束,O(全服规则) 可接受);listener 集中 `GameEventListenerHub` 一个类(MONITOR 优先级,kill=PlayerDeathEvent killer≠null) | scripting.md §3 |
| K16 | **条件保存期预 parse**:create/update 校验链加"所有 if.condition 过 ExpressionParser.parse",失败 → SCRIPT_INVALID(parse 错误信息首行);入口 `ConditionEvaluator.checkSyntax(condition)` 静态方法 | 批次1 质量审查 M-3:坏条件别等运行期静默 false |
| K17 | **playerJoin/playerKill/playerNear 触发的脚本带"事件玩家"上下文**:TriggerContext.detail = 玩家名;**v1 不做 `{player}` 变量注入到动作**(runCommand 的 online-player 参数由规则作者显式填;事件玩家注入留 P5+ 评估) | 防 scope 膨胀;Scratch 语义里"谁触发的"是高级需求 |

## 批次 A:命令模板 + 试跑轨迹 + 预 parse(2 commit)

**A1 命令模板**:
- `HikariCanvasConfig`:`scripts.command-templates` 段 → `record CommandTemplate(String command, Map<String, ParamSpec> params)` / `record ParamSpec(int maxLength, String type)`(type: "text"(默)|"online-player");ScriptsConfig 加 `Map<String, CommandTemplate> commandTemplates`
- 新 `script/engine/CommandTemplateEngine.java`:`render(templateId, params, onlinePlayerNames) → Result(ok command | error 原因)`——纯函数可测:模板查无/参数缺失/超长/转义(剥换行 `§`、`@` 拒)/online-player 校验
- `ActionExecutor.runCommand` 真实化:engine.render → 主线程 hop `Bukkit.dispatchCommand(console, cmd)` + audit `SCRIPT_COMMAND_EXECUTED`(templateId+替换后全文+ruleKey);render 失败 → error/blocked step
- config.yml 注释段(带 announce 示例,注释掉)
- 测试:CommandTemplateEngine 全场景 ≥10 case + ActionExecutor runCommand 接线 case

**A2 试跑轨迹(K11/K12)+ 预 parse(K16)**:
- `ScriptRunner.submit` 重载加 `Consumer<List<TraceStep>> traceCallback`(run 结束(含 wait 续接最终段)回调;blocked/掐断也回调)
- `ScriptOpDispatcher.handleTest` 改:不再走 ScriptTestSeam 同步返回——经新 seam `ScriptTestLauncher { void launch(wallId, ruleId, Consumer<List<TraceStep>>) }`;ack 立即 `{accepted:true, ruleId}`;callback 里把 steps 转 wire(List<Map>)经 OpPushCallback 推 **新 Envelope op `script.trace`**`{ruleId, steps}` 给本墙 session(找 push 通道现成方法;session 没了静默丢)。删旧 ScriptTestSeam(P1 占位,无外部消费)
- 装配:HikariCanvas 把 launcher 接 runner.submit(TriggerContext(TEST,0,"test"))
- K16:`ConditionEvaluator.checkSyntax(String condition) → Optional<String>`(parse-only);ScriptOpDispatcher 校验链在 Validator 后逐 if.condition 调
- 前端最小接线:wsClient 认识 `script.trace`(收到先 pushLog meta + 存 scripts store `lastTrace` ref;P5 积木高亮消费)+ `sendScriptTest` ack 形态更新注释
- 文档:protocol.md §5.14 script.test 行改异步语义 + 新增 script.trace 推送行;scripting.md §4.2 同步
- 测试:dispatcher test 异步 ack 形态 / trace 推送 wire 形态 / checkSyntax 拒坏条件(create 被 SCRIPT_INVALID)/ 前端 store lastTrace case

## 批次 B:游戏事件层(1-2 commit)

**B1 join/kill + Router 全局索引(K15)**:
- `script/engine/GameEventListenerHub.java`(implements Listener):`@EventHandler(priority=MONITOR, ignoreCancelled=true)` PlayerJoinEvent → `router.firePlayerJoin(playerName)`;PlayerDeathEvent(killer≠null)→ `router.firePlayerKill(victimName, killerName)`
- TriggerRouter:`byGlobal: Map<Class<? extends Trigger>, Set<RuleRef>>` 或两个字段;rebuild 维护;fire 方法遍历 submit(TriggerContext(VARIABLE→新枚举 PLAYER_JOIN/PLAYER_KILL?——**TriggerContext.Source 加 PLAYER_JOIN/PLAYER_KILL/PLAYER_NEAR 三枚举**,detail=玩家名(kill 用 "victim→killer"))
- 装配:onEnable `Bukkit.getPluginManager().registerEvents(hub, this)`
- 测试:Router 全局索引 rebuild/触发/disabled 跳过(fake);Hub 用 fake router 验证转发(Bukkit event 构造太重就抽 hub 的内部转发逻辑测)

**B2 playerNear 采样器(K14)**:
- `script/engine/PlayerNearSampler.java`:依赖注入(WallSource 抽象拿墙原点(照 TimelineTriggerRegistry.WallSource 范式扩或新建)/ Router / 在线玩家源抽象 `Supplier<Collection<PlayerPos>>`(record PlayerPos(name, worldId, x, y, z))——**Bukkit 读包在装配层 lambda,Sampler 本体零 Bukkit 可单测**)
- 状态机:`Map<String playerName+"|"+ruleKey, Boolean inside>`;tick():对每个 near 规则 × 每玩家:同世界 + 距离平方 ≤ range² → 进入沿 submit;离开重置;玩家不在线清条目(每 tick 顺手清离线)
- Router:near 规则索引 `Map<wallId, List<(ruleId, range)>>` rebuild 维护 + `nearRules()` 快照给 Sampler
- 装配:`Bukkit.getScheduler().runTaskTimer(plugin, sampler::tick, interval, interval)`(主线程);config `scripts.player-near-sample-ticks` 默 10(clamp 1..200)+ reload 热更(重 schedule 或 sampler 读 volatile——选 volatile 间隔由 task 自判跳帧,免重 schedule)
- 测试:状态机全沿(进入触发/停留不重复/离开重置/再进入再触发/跨世界不触发/边界 16²)/ 离线清理 / disabled 规则
- 收尾:全量回归 + journal(P3 收口我写)

## 收口(我做)
scripting.md §8 P3 ✅ + §11 ScriptTestSeam 勾账 + §10 采样间隔回填 config 名 + protocol.md 核对 + journal + CLAUDE.md + 全量回归。
