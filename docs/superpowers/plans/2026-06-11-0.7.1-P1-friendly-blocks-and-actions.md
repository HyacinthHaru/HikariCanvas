# 0.7.1-P1 友好元素积木 + 低风险新动作 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给 0.7.0 脚本积木编辑器加 8 个友好元素积木（移到/改大小/旋转/透明度/显示/隐藏/改文字/改颜色）+ 相对移动 + 4 个低风险新动作（发消息/设随机数/变量乘除/播时间轴等播完），后端加 6 个新 Action 子类。

**Architecture:** 友好积木走「路线乙」——新增后端复合 action `setElementProperties(elementId, patch, kind)`，一个友好积木序列化成一条 action，守住 0.7.0 的「1 积木 = 1 条 action」blockId 同构（试跑高亮 / undo / P4 幽灵拖动天然正确）。`kind` 是前端皮肤标记，后端执行忽略。相对移动走 `nudgeElement`（运行时读当前值 + 增量）。`playTimelineAwait` 经 `ActionSink.timelineDurationMs` + Runner 特判复用 wait 续接挂起。

**Tech Stack:** Java 21（sealed Action + 手写 Jackson (De)serializer）/ Vue 3 + TS（blockDefs 声明式 + BlockNode 友好皮肤渲染）/ JUnit 5（纯 fake）/ vitest + @vue/test-utils。

**契约：** `docs/scripting-0.7.1.md`（E6 已回填路线乙）+ `docs/scripting.md`（0.7.0 基座）。

**协议版本：** P1 **不升** SUPPORTED_MIN/MAX（仍 v4），仅加 action 子类；v5 正式升版在 P2。前后端同 build 部署，P1 新 action 即可用。

---

## 6 个新 Action 子类一览（贯穿全计划）

| wireType | record 字段 | 执行 | 权限面 |
|---|---|---|---|
| `setElementProperties` | `(String elementId, Map<String,String> patch, String kind)` | `ElementPropertyApplier.applyMany` | edit |
| `nudgeElement` | `(String elementId, double dx, double dy)` | `ElementPropertyApplier.applyNudge`（读当前 x/y + delta） | edit |
| `sendMessage` | `(String text, String channel)` channel∈chat/actionbar/title | 主线程 hop，给触发玩家（TRIGGER_DETAIL ThreadLocal） | edit |
| `setRandomVariable` | `(String fullName, double min, double max)` | async，RNG seam → store.setValue | edit |
| `scaleVariable` | `(String fullName, String op, double factor)` op∈multiply/divide | async，storeLookup 读当前 × / ÷ | edit |
| `playTimelineAwait` | `(String timelineId)` | ticker.play + Runner 挂起 durationMs | edit |

**每个新 action 的「后端接线清单」（编译器穷举守门，缺一处不编译）：**
1. `Action.java`：`permits` 加子类 + `record` 定义 + `wireType()`
2. `ActionDeserializer.java`：`switch(type)` 加 case
3. `ActionSerializer.java`：`switch(value)` 加 case
4. `ActionExecutor.java`：`execute` switch 加 case + `doXxx` 方法
5. `ScriptRuleValidator.java`：`validateAction` switch 加 case（+ 常量）
6. `ScriptPermissions.java`：`scanActions` switch 加 case（本批全 `-> { }` 无附加面）

**前端接线清单：**
7. `web/src/types/protocol.ts`：`ScriptAction` union 加成员
8. `web/src/script/model/blockDefs.ts`：blockDef + `makeDefaultAction` case
9. `web/src/script/model/validator.ts`：`validateAction` case（+ 常量）
10. `web/src/i18n/messages.ts`：中英 key

> **⚠️ 编译依赖（执行者必读）：** sealed `Action` 加 record 后，`ActionExecutor.execute` /
> `ScriptRuleValidator.validateAction` / `ScriptPermissions.scanActions` / `ActionSerializer`
> 这 4 处**穷尽 switch（无 default）立即编译失败**直到补全对应 case。所以后端虽按 Task 分列，
> 实施时须**一次性加全 6 个 record + 4 处 switch 的全部 case** 再编译，合理合并成 2-3 个
> **每个都可编译**的 commit（如：契约+wire / 执行+校验+权限 / 测试）。不要按单 Task 逐 commit
> 留中间编译失败态。`ActionDeserializer`（switch on String，有 default）不强制，但功能需要补 case。

---

## 批次 1：后端数据契约（6 个 Action 子类 + wire）

### Task 1: Action.java 加 6 个 record + (De)serializer + wire 单测

**Files:**
- Modify: `plugin/src/main/java/moe/hikari/canvas/script/Action.java`
- Modify: `plugin/src/main/java/moe/hikari/canvas/script/ActionDeserializer.java`
- Modify: `plugin/src/main/java/moe/hikari/canvas/script/ActionSerializer.java`
- Test: `plugin/src/test/java/moe/hikari/canvas/script/ActionWireTest.java`

- [ ] **Step 1: 写失败测试** —— 在 `ActionWireTest` 加 6 个 round-trip 用例。范式（照文件里现有 `setVariable` round-trip 写法；逐个 action 各一条）：

```java
@Test
void setElementProperties_roundTrip() throws Exception {
    Action a = new Action.SetElementProperties(
            "e-1", java.util.Map.of("x", "128", "y", "64"), "moveTo");
    String json = MAPPER.writeValueAsString(a);
    Action back = MAPPER.readValue(json, Action.class);
    assertEquals(a, back);
    assertTrue(json.contains("\"type\":\"setElementProperties\""));
}

@Test
void nudgeElement_roundTrip() throws Exception {
    Action a = new Action.NudgeElement("e-1", 5.0, -3.0);
    assertEquals(a, MAPPER.readValue(MAPPER.writeValueAsString(a), Action.class));
}

@Test
void sendMessage_roundTrip() throws Exception {
    Action a = new Action.SendMessage("hi ${var:user/name}", "actionbar");
    assertEquals(a, MAPPER.readValue(MAPPER.writeValueAsString(a), Action.class));
}

@Test
void setRandomVariable_roundTrip() throws Exception {
    Action a = new Action.SetRandomVariable("user/roll", 1.0, 6.0);
    assertEquals(a, MAPPER.readValue(MAPPER.writeValueAsString(a), Action.class));
}

@Test
void scaleVariable_roundTrip() throws Exception {
    Action a = new Action.ScaleVariable("user/score", "multiply", 2.0);
    assertEquals(a, MAPPER.readValue(MAPPER.writeValueAsString(a), Action.class));
}

@Test
void playTimelineAwait_roundTrip() throws Exception {
    Action a = new Action.PlayTimelineAwait("t-1");
    assertEquals(a, MAPPER.readValue(MAPPER.writeValueAsString(a), Action.class));
}
```

- [ ] **Step 2: 跑测试确认失败** —— `./gradlew :plugin:test --tests "*ActionWireTest"`，预期编译失败（record 未定义）。

- [ ] **Step 3: Action.java 加 permits + 6 record** —— `permits` 列表加 6 个名字；record 体（放在 `If` 之后、接口尾 `}` 之前）：

```java
    /** 0.7.1：批量设元素属性（友好积木的序列化目标）。patch 键 = 属性名（白名单），
     * 值 = 字符串值；kind = 前端皮肤标记（moveTo/resize/...），后端执行忽略，仅透传存储。 */
    record SetElementProperties(String elementId, java.util.Map<String, String> patch,
                                String kind) implements Action {
        @Override public String wireType() { return "setElementProperties"; }
        public SetElementProperties {
            patch = patch == null ? java.util.Map.of() : java.util.Map.copyOf(patch);
        }
    }

    /** 0.7.1：相对移动元素（运行时读当前 x/y + 增量）。 */
    record NudgeElement(String elementId, double dx, double dy) implements Action {
        @Override public String wireType() { return "nudgeElement"; }
    }

    /** 0.7.1：给触发该脚本的玩家发消息。channel = chat|actionbar|title。 */
    record SendMessage(String text, String channel) implements Action {
        @Override public String wireType() { return "sendMessage"; }
    }

    /** 0.7.1：设随机数变量（min..max 闭区间均匀采样，输出整数若两端皆整）。 */
    record SetRandomVariable(String fullName, double min, double max) implements Action {
        @Override public String wireType() { return "setRandomVariable"; }
    }

    /** 0.7.1：变量乘 / 除（读当前值 × / ÷ factor）。op = multiply|divide。 */
    record ScaleVariable(String fullName, String op, double factor) implements Action {
        @Override public String wireType() { return "scaleVariable"; }
    }

    /** 0.7.1：播放时间轴并等播完（等一轮 durationMs，由 Runner 挂起续接）。 */
    record PlayTimelineAwait(String timelineId) implements Action {
        @Override public String wireType() { return "playTimelineAwait"; }
    }
```

permits 最终形态：
```java
public sealed interface Action permits
        Action.SetVariable, Action.IncrementVariable, Action.SetElementProperty,
        Action.PlayTimeline, Action.PlaySound, Action.Wait,
        Action.RunCommand, Action.Log, Action.If,
        Action.SetElementProperties, Action.NudgeElement, Action.SendMessage,
        Action.SetRandomVariable, Action.ScaleVariable, Action.PlayTimelineAwait {
```

- [ ] **Step 4: ActionDeserializer 加 6 case** —— 在 `switch (type)` 的 `default` 之前插入。`setElementProperties` 的 patch 用一个 helper（照文件里 `readParams` 范式新增 `readStringMap(ctxt, node, "patch", type)`，把 JSON object 读成 `Map<String,String>`；缺字段返空 map）：

```java
case "setElementProperties" -> new Action.SetElementProperties(
        requireText(ctxt, node, "elementId", type),
        readStringMap(ctxt, node, "patch", type),
        optionalText(ctxt, node, "kind", type));   // kind 可空 → optionalText 返 "" 当 null
case "nudgeElement" -> new Action.NudgeElement(
        requireText(ctxt, node, "elementId", type),
        requireDouble(ctxt, node, "dx", type),
        requireDouble(ctxt, node, "dy", type));
case "sendMessage" -> new Action.SendMessage(
        requireText(ctxt, node, "text", type),
        requireText(ctxt, node, "channel", type));
case "setRandomVariable" -> new Action.SetRandomVariable(
        requireText(ctxt, node, "fullName", type),
        requireDouble(ctxt, node, "min", type),
        requireDouble(ctxt, node, "max", type));
case "scaleVariable" -> new Action.ScaleVariable(
        requireText(ctxt, node, "fullName", type),
        requireText(ctxt, node, "op", type),
        requireDouble(ctxt, node, "factor", type));
case "playTimelineAwait" -> new Action.PlayTimelineAwait(
        requireText(ctxt, node, "timelineId", type));
```

`readStringMap` / `optionalText` 助手（若文件里无 `optionalText`，照 `requireText` 加一个返回 `node.has(field) ? node.get(field).asText() : null` 的版本；`readStringMap` 照 `readParams`：遍历 object 字段 put 进 LinkedHashMap，非 object 返 `Map.of()`）。

- [ ] **Step 5: ActionSerializer 加 6 case** —— 在 `switch (value)` 里加（`If` 之后）：

```java
case Action.SetElementProperties a -> {
    gen.writeStringField("elementId", a.elementId());
    gen.writeObjectFieldStart("patch");
    for (Map.Entry<String, String> e : a.patch().entrySet()) {
        gen.writeStringField(e.getKey(), e.getValue());
    }
    gen.writeEndObject();
    gen.writeStringField("kind", a.kind() == null ? "" : a.kind());
}
case Action.NudgeElement a -> {
    gen.writeStringField("elementId", a.elementId());
    gen.writeNumberField("dx", a.dx());
    gen.writeNumberField("dy", a.dy());
}
case Action.SendMessage a -> {
    gen.writeStringField("text", a.text());
    gen.writeStringField("channel", a.channel());
}
case Action.SetRandomVariable a -> {
    gen.writeStringField("fullName", a.fullName());
    gen.writeNumberField("min", a.min());
    gen.writeNumberField("max", a.max());
}
case Action.ScaleVariable a -> {
    gen.writeStringField("fullName", a.fullName());
    gen.writeStringField("op", a.op());
    gen.writeNumberField("factor", a.factor());
}
case Action.PlayTimelineAwait a -> gen.writeStringField("timelineId", a.timelineId());
```

- [ ] **Step 6: 跑测试确认通过** —— `./gradlew :plugin:test --tests "*ActionWireTest"`，预期 6 个新用例全绿。`kind` round-trip 注意：`SetElementProperties(..., null)` 序列化成 `"kind":""`，反序列化 `optionalText` 返 null → record 存 null。断言用 `kind=""` 构造或允许 null↔"" 折叠（测试里统一用非空 kind `"moveTo"` 避开歧义；额外加一条 `kind=null` 序列化成 `""` 再读回 `null` 的 round-trip 断言宽松版）。

- [ ] **Step 7: Commit** —— `git add` 上述 4 文件，commit `0.7.1-P1-T1: 6 个新 Action 子类 + wire (setElementProperties/nudge/sendMessage/setRandom/scale/playTimelineAwait)`，push。

---

## 批次 2：后端执行 + 校验 + 权限

### Task 2: ElementPropertyApplier 加 applyMany + applyNudge

**Files:**
- Modify: `plugin/src/main/java/moe/hikari/canvas/script/engine/ElementPropertyApplier.java`
- Modify: 生产 seam 实现 `plugin/src/main/java/moe/hikari/canvas/session/SessionManager.java`（`applyScriptElementPatch` 旁加 `applyScriptElementNudge`；实现者先 grep `applyScriptElementPatch` 定位装配点）
- Test: `plugin/src/test/java/moe/hikari/canvas/script/engine/ElementPropertyApplierTest.java`

- [ ] **Step 1: 写失败测试** —— 加用例：① `applyMany` headless 路径设 `{x,y}` 两属性 → fake WallRepo 的 state 元素 x/y 都变；② `applyNudge` headless：元素初始 x=10,y=20，nudge(+5,-3) → x=15,y=17；③ `applyMany` patch 含非法属性 → error step（buildPatch 抛）。照文件现有 headless 测试范式（fake WallRepo + 断言 updateState 后 state）。

```java
@Test
void applyMany_headless_setsMultipleProps() {
    // fake wallRepo 持一个 x=0,y=0 的 RectElement，无 session（sessionApplier=null）
    ElementPropertyApplier applier = new ElementPropertyApplier(null, fakeRepo, null, LOG);
    TraceStep step = applier.applyMany(WALL, "b", "e-1",
            java.util.Map.of("x", "128", "y", "64"));
    assertEquals("ok", step.result());
    Element el = findElement(fakeRepo.loadById(WALL).orElseThrow().state(), "e-1");
    assertEquals(128, el.x());
    assertEquals(64, el.y());
}

@Test
void applyNudge_headless_readsCurrentPlusDelta() {
    // fake wallRepo 持 x=10,y=20 元素
    ElementPropertyApplier applier = new ElementPropertyApplier(null, fakeRepo, null, LOG);
    TraceStep step = applier.applyNudge(WALL, "b", "e-1", 5.0, -3.0);
    assertEquals("ok", step.result());
    Element el = findElement(fakeRepo.loadById(WALL).orElseThrow().state(), "e-1");
    assertEquals(15, el.x());
    assertEquals(17, el.y());
}
```

- [ ] **Step 2: 跑测试确认失败** —— 方法未定义，编译失败。

- [ ] **Step 3: 重构出 applyPatch 私有方法 + 加 applyMany** —— 把现有 `apply` 的「路径 A/路径 B 落 patch」逻辑抽成私有 `applyPatch(wallId, blockId, elementId, Map<String,Object> patch, String desc)`，`apply`（单属性）改成 `applyPatch(wallId, blockId, elementId, buildPatch(property,value), property)`。新增 `applyMany`：

```java
/** 0.7.1：批量设属性（friendly 积木）。patch 每个 (key,val) 过 buildPatch 合并成一个
 * element.update patch，一次落地（同 session/headless 双路径）。任一键非法 → error step。 */
public TraceStep applyMany(String wallId, String blockId, String elementId,
                           Map<String, String> rawPatch) {
    if (elementId == null || elementId.isEmpty()) {
        return TraceStep.error(blockId, "elementId 缺失");
    }
    if (rawPatch == null || rawPatch.isEmpty()) {
        return TraceStep.error(blockId, "patch 为空");
    }
    Map<String, Object> merged = new java.util.LinkedHashMap<>();
    try {
        for (Map.Entry<String, String> e : rawPatch.entrySet()) {
            merged.putAll(buildPatch(e.getKey(), e.getValue()));
        }
    } catch (IllegalArgumentException ex) {
        return TraceStep.error(blockId, ex.getMessage());
    }
    return applyPatch(wallId, blockId, elementId, merged, "patch" + merged.keySet());
}
```

- [ ] **Step 4: 加 applyNudge + 扩 SessionPatchApplier seam** —— `SessionPatchApplier` 接口加 nudge 方法（带 default 让既有 fake 不破）：

```java
public interface SessionPatchApplier {
    SessionOutcome apply(String wallId, String elementId, Map<String, Object> patch);

    /** 0.7.1 nudge 原子：session 锁内读当前 x/y + delta 写回。无 session 返 noSession。
     * default = noSession（旧 fake 自动走 headless）。 */
    default SessionOutcome nudge(String wallId, String elementId, int dx, int dy) {
        return SessionOutcome.noSession();
    }
}
```

`applyNudge`（dx/dy double → round 成 int delta；session 优先，否则 headless 读 state x/y + delta）：

```java
/** 0.7.1：相对移动。dx/dy round 成 int 增量。session 路径锁内原子读改写；
 * headless 路径读 DB state 当前 x/y + 增量（已知低危竞态同 applyHeadless 注释）。 */
public TraceStep applyNudge(String wallId, String blockId, String elementId,
                            double dx, double dy) {
    if (elementId == null || elementId.isEmpty()) {
        return TraceStep.error(blockId, "elementId 缺失");
    }
    if (!Double.isFinite(dx) || !Double.isFinite(dy)) {
        return TraceStep.error(blockId, "dx/dy 必须有限");
    }
    int idx = (int) Math.round(dx);
    int idy = (int) Math.round(dy);
    // 路径 A：session 原子 nudge
    if (sessionApplier != null) {
        SessionOutcome outcome;
        try {
            outcome = sessionApplier.nudge(wallId, elementId, idx, idy);
        } catch (RuntimeException e) {
            log.log(Level.WARNING, "[脚本] session nudge 异常: " + e.getMessage(), e);
            outcome = SessionOutcome.failed(String.valueOf(e.getMessage()));
        }
        if (outcome != null) {
            switch (outcome.status()) {
                case APPLIED -> { return TraceStep.ok(blockId, "action",
                        "nudge → session(+" + idx + "," + idy + ")"); }
                case FAILED -> { return TraceStep.error(blockId, String.valueOf(outcome.detail())); }
                case NO_SESSION -> { /* fall through headless */ }
            }
        }
    }
    // 路径 B：headless 读 state 当前 x/y
    if (wallRepo == null) {
        return TraceStep.error(blockId, "headless 路径不可用（WallRepo 未装配）");
    }
    WallRepo.Wall wall = wallRepo.loadById(wallId).orElse(null);
    if (wall == null || wall.state() == null) {
        return TraceStep.error(blockId, "wall 不存在或无 state: " + wallId);
    }
    Integer curX = null, curY = null;
    for (var layer : wall.state().layers()) {
        for (var el : layer.elements()) {
            if (el.id().equals(elementId)) { curX = el.x(); curY = el.y(); }
        }
    }
    if (curX == null) {
        return TraceStep.error(blockId, "元素不存在: " + elementId);
    }
    return applyMany(wallId, blockId, elementId,
            Map.of("x", String.valueOf(curX + idx), "y", String.valueOf(curY + idy)));
}
```

- [ ] **Step 5: 生产 seam 实现 applyScriptElementNudge** —— `SessionManager` 里 `applyScriptElementPatch` 旁实现 nudge：定位活跃 session → 无则返 `SessionOutcome.noSession()`；有则在 session 内读元素当前 x/y（`session.state()` 找 element）+ delta，调与 patch 路径相同的 `updateElement(elementId, Map.of("x",x+dx,"y",y+dy))` 标准链，返 applied/failed。装配处把这个方法绑给 `ElementPropertyApplier` 的 `SessionPatchApplier`（实现者照现有 lambda/匿名类装配点扩 nudge override）。

- [ ] **Step 6: 跑测试确认通过** —— `./gradlew :plugin:test --tests "*ElementPropertyApplierTest"`。

- [ ] **Step 7: Commit** —— `0.7.1-P1-T2: ElementPropertyApplier applyMany + applyNudge（扩 SessionPatchApplier nudge seam）`，push。

### Task 3: ActionExecutor 加 6 个 do 方法 + RNG seam + 触发玩家透传

**Files:**
- Modify: `plugin/src/main/java/moe/hikari/canvas/script/engine/ActionExecutor.java`
- Modify: `plugin/src/main/java/moe/hikari/canvas/script/engine/ActionSink.java`（加 `timelineDurationMs` default 方法）
- Test: `plugin/src/test/java/moe/hikari/canvas/script/engine/ActionExecutorTest.java`

- [ ] **Step 1: 写失败测试** —— 加用例：
  - `doSetElementProperties` → applier.applyMany 被调（fake applier 记录 patch）
  - `doNudge` → applier.applyNudge 被调（dx/dy 透传）
  - `doSendMessage`：plugin=null 直跑路径，TRIGGER_DETAIL=null 时返 ok step「无触发玩家」；TRIGGER_DETAIL 有值但玩家离线 → ok step skip
  - `doSetRandom`：注入 seeded RNG，min=1 max=1 → 变量恒 "1"；min=0 max=10 结果 ∈ [0,10]
  - `doScale`：变量当前 "10"，multiply factor=2 → "20"；divide factor=0 → error step；divide factor=2 → "5"
  - `doPlayTimelineAwait`：fake ticker.play 返 OK → ok step；play 失败 → error step

```java
@Test
void scaleVariable_multiply() {
    store.create("user:w-1", "score", VarType.NUMBER, "10", "manual");
    TraceStep step = executor().execute(WALL, "b",
            new Action.ScaleVariable("user/score", "multiply", 2.0));
    assertEquals("ok", step.result());
    assertEquals("20", valueOf("user:w-1/score"));
}

@Test
void scaleVariable_divideByZero_errors() {
    store.create("user:w-1", "score", VarType.NUMBER, "10", "manual");
    TraceStep step = executor().execute(WALL, "b",
            new Action.ScaleVariable("user/score", "divide", 0.0));
    assertEquals("error", step.result());
}

@Test
void setRandomVariable_seeded_deterministic() {
    store.create("user:w-1", "roll", VarType.NUMBER, "0", "manual");
    // executor 注入 seeded RandomGenerator（见 Step 3 seam）
    TraceStep step = executorWithRng(new java.util.Random(42)).execute(WALL, "b",
            new Action.SetRandomVariable("user/roll", 1.0, 1.0));
    assertEquals("ok", step.result());
    assertEquals("1", valueOf("user:w-1/roll"));
}
```

- [ ] **Step 2: 跑测试确认失败**。

- [ ] **Step 3: ActionExecutor 加字段 + execute case + do 方法** ——
  - 加字段 `private final java.util.random.RandomGenerator rng;`，9 参构造尾初始化 `this.rng = java.util.concurrent.ThreadLocalRandom.current();`，并加一个**包级**测试构造或包级 setter `void setRngForTest(RandomGenerator r)`（测试注 seeded）。
  - `execute` switch 加 6 case（编译器强制）：

```java
case Action.SetElementProperties a -> doSetElementProperties(wallId, blockId, a);
case Action.NudgeElement a -> doNudge(wallId, blockId, a);
case Action.SendMessage a -> doSendMessage(wallId, blockId, a);
case Action.SetRandomVariable a -> doSetRandom(wallId, blockId, a);
case Action.ScaleVariable a -> doScale(wallId, blockId, a);
case Action.PlayTimelineAwait a -> doPlayTimelineAwait(wallId, blockId, a);
```

  - do 方法：

```java
private TraceStep doSetElementProperties(String wallId, String blockId,
                                         Action.SetElementProperties a) {
    if (applier == null) return TraceStep.error(blockId, "ElementPropertyApplier 未装配");
    return applier.applyMany(wallId, blockId, a.elementId(), a.patch());
}

private TraceStep doNudge(String wallId, String blockId, Action.NudgeElement a) {
    if (applier == null) return TraceStep.error(blockId, "ElementPropertyApplier 未装配");
    return applier.applyNudge(wallId, blockId, a.elementId(), a.dx(), a.dy());
}

/** 给触发玩家发消息。触发玩家名 = ScriptRunner.TRIGGER_DETAIL（仅 player* 触发器有值）。
 * 无触发玩家 / 离线 → ok step skip（非错误）。text 过 ${var:X} 插值。主线程 hop 发。 */
private TraceStep doSendMessage(String wallId, String blockId, Action.SendMessage a) {
    String channel = a.channel();
    if (!"chat".equals(channel) && !"actionbar".equals(channel) && !"title".equals(channel)) {
        return TraceStep.error(blockId, "未知 channel: " + channel);
    }
    String who = ScriptRunner.currentTriggerDetail();
    if (who == null || who.isBlank()) {
        return TraceStep.ok(blockId, "action", "无触发玩家，跳过");
    }
    String msg = interpolator == null ? a.text() : interpolator.interpolate(a.text(), wallId).text();
    Runnable work = () -> {
        try {
            Player p = Bukkit.getPlayerExact(who);
            if (p == null) return; // 离线
            switch (channel) {
                case "actionbar" -> p.sendActionBar(net.kyori.adventure.text.Component.text(msg));
                case "title" -> p.showTitle(net.kyori.adventure.title.Title.title(
                        net.kyori.adventure.text.Component.text(msg),
                        net.kyori.adventure.text.Component.empty()));
                default -> p.sendMessage(net.kyori.adventure.text.Component.text(msg));
            }
        } catch (Throwable t) {
            log.log(Level.WARNING, "[脚本] sendMessage 失败: " + t.getMessage(), t);
        }
    };
    if (plugin == null) work.run(); else Bukkit.getScheduler().runTask(plugin, work);
    return TraceStep.ok(blockId, "action", "msg→" + who + " (" + channel + ")");
}

/** 随机数 [min,max] 闭区间。min>max → error。输出复用 formatNumber（两端整 → 无小数）。 */
private TraceStep doSetRandom(String wallId, String blockId, Action.SetRandomVariable a) {
    if (store == null) return TraceStep.error(blockId, "VariableStore 未装配");
    if (!Double.isFinite(a.min()) || !Double.isFinite(a.max()) || a.min() > a.max()) {
        return TraceStep.error(blockId, "随机区间非法: " + a.min() + ".." + a.max());
    }
    double v = a.min() + rng.nextDouble() * (a.max() - a.min());
    // 两端皆整 → 取整数随机（含 max）：roll dice 体验
    if (a.min() == Math.rint(a.min()) && a.max() == Math.rint(a.max())) {
        v = a.min() + rng.nextInt((int) (a.max() - a.min()) + 1);
    }
    String fullName = VariableInterpolator.resolveFullName(a.fullName(), wallId);
    try {
        store.setValue(fullName, formatNumber(v), null);
    } catch (VariableException e) {
        return TraceStep.error(blockId, "setRandom " + fullName + ": " + e.getMessage());
    }
    return TraceStep.ok(blockId, "action", fullName + " = " + formatNumber(v));
}

/** 变量乘 / 除。读当前值（StrictNumber，非数值作 0）→ × / ÷ → 写回。divide by 0 → error。 */
private TraceStep doScale(String wallId, String blockId, Action.ScaleVariable a) {
    if (store == null || storeLookup == null) return TraceStep.error(blockId, "VariableStore 未装配");
    boolean mul = "multiply".equals(a.op());
    if (!mul && !"divide".equals(a.op())) return TraceStep.error(blockId, "未知 op: " + a.op());
    if (!Double.isFinite(a.factor())) return TraceStep.error(blockId, "factor 必须有限");
    if (!mul && a.factor() == 0.0) return TraceStep.error(blockId, "除数不能为 0");
    String fullName = VariableInterpolator.resolveFullName(a.fullName(), wallId);
    double base = StrictNumber.parse(storeLookup.apply(fullName));
    double result = mul ? base * a.factor() : base / a.factor();
    try {
        store.setValue(fullName, formatNumber(result), null);
    } catch (VariableException e) {
        return TraceStep.error(blockId, "scaleVariable " + fullName + ": " + e.getMessage());
    }
    return TraceStep.ok(blockId, "action", fullName + " = " + formatNumber(result));
}

/** 播时间轴：触发 play（挂起 durationMs 由 Runner 经 timelineDurationMs 处理）。 */
private TraceStep doPlayTimelineAwait(String wallId, String blockId, Action.PlayTimelineAwait a) {
    if (ticker == null) return TraceStep.error(blockId, "AnimationTicker 未装配");
    if (a.timelineId() == null || a.timelineId().isBlank()) {
        return TraceStep.error(blockId, "缺 timelineId");
    }
    AnimationTicker.Result r = ticker.play(wallId, a.timelineId());
    if (r != AnimationTicker.Result.OK) return TraceStep.error(blockId, "play 失败: " + r);
    return TraceStep.ok(blockId, "action", "playAwait " + a.timelineId());
}
```

- [ ] **Step 4: ActionExecutor 实现 ActionSink.timelineDurationMs** —— 从 wallRepo 读 timeline 总时长（查无返 0，Runner 据此决定不挂起）：

```java
@Override
public long timelineDurationMs(String wallId, String timelineId) {
    if (wallRepo == null || timelineId == null) return 0L;
    WallRepo.Wall wall = wallRepo.loadById(wallId).orElse(null);
    if (wall == null || wall.state() == null) return 0L;
    for (var tl : wall.state().timelines()) {
        if (tl.id().equals(timelineId)) {
            return Math.max(0L, Math.min(tl.durationMs(), 600_000L)); // 封顶 10 分钟防异常
        }
    }
    return 0L;
}
```

- [ ] **Step 5: ActionSink 加 timelineDurationMs default 方法**：

```java
    /** 0.7.1 playTimelineAwait：返回时间轴一轮时长（ms），>0 时 Runner 挂起续接。
     * 查无 / 不支持 → 0（不挂起）。default 0 让既有 fake 不破。 */
    default long timelineDurationMs(String wallId, String timelineId) {
        return 0L;
    }
```

- [ ] **Step 6: 跑测试确认通过** —— `./gradlew :plugin:test --tests "*ActionExecutorTest"`。

- [ ] **Step 7: Commit** —— `0.7.1-P1-T3: ActionExecutor 6 do 方法 + RNG seam + timelineDurationMs`，push。

### Task 4: ScriptRunner playTimelineAwait 特判 + 触发玩家 ThreadLocal

**Files:**
- Modify: `plugin/src/main/java/moe/hikari/canvas/script/engine/ScriptRunner.java`
- Test: `plugin/src/test/java/moe/hikari/canvas/script/engine/ScriptRunnerTest.java`

- [ ] **Step 1: 写失败测试** —— ① playTimelineAwait 后接一个 log：fake sink `timelineDurationMs` 返 1000，用同步调度替身验证「play step 后挂起，调度 1000ms 续接，续接里执行 log」；②`timelineDurationMs` 返 0（查无）→ 不挂起，log 紧接执行；③ TRIGGER_DETAIL：submit 带 `TriggerContext(PLAYER_JOIN, 0, "Alice")` → run 内 `ScriptRunner.currentTriggerDetail()` == "Alice"，run 结束后清空（null）。

- [ ] **Step 2: 跑测试确认失败**。

- [ ] **Step 3: 加 TRIGGER_DETAIL ThreadLocal**（照 RULE_KEY 范式，包级）：

```java
/** 0.7.1：当前 run 的触发玩家名（TriggerContext.detail，仅 player* 触发器有值）。
 * 与 RULE_KEY 同生命周期（runFrames 置位 / finally 清）。ActionExecutor.doSendMessage 读它。 */
static final ThreadLocal<String> TRIGGER_DETAIL = new ThreadLocal<>();

/** 包级读取入口：当前线程触发玩家名；无 run 上下文返 null。 */
static @Nullable String currentTriggerDetail() {
    return TRIGGER_DETAIL.get();
}
```

  在 `runFrames` 开头 `RULE_KEY.set(...)` 旁加 `TRIGGER_DETAIL.set(st.ctx.detail());`，`finally` 里加 `TRIGGER_DETAIL.remove();`。

- [ ] **Step 4: runFrames 加 playTimelineAwait 特判** —— 在 `if (a instanceof Action.Wait wt)` 块之后、普通动作 `sink.execute` 之前插入：

```java
if (a instanceof Action.PlayTimelineAwait pta) {
    // 先 play（副作用走 sink），再据 durationMs 决定是否挂起续接（复用 wait 范式）
    TraceStep step = sink.execute(st.wallId, blockId, pta);
    st.trace.add(step == null ? TraceStep.error(blockId, "null step") : step);
    long dur = "ok".equals(step != null ? step.result() : "error")
            ? sink.timelineDurationMs(st.wallId, pta.timelineId()) : 0L;
    if (dur > 0) {
        stack.push(new Frame(acts, i + 1, f.prefix()));
        Deque<Frame> cont = new ArrayDeque<>(stack);
        if (!shutdown) {
            try {
                scheduler.schedule(() -> runFrames(st, cont), dur);
            } catch (RejectedExecutionException e) { /* shutdown 竞态 */ }
        }
        return;
    }
    i++; // 不挂起（play 失败 / 查无 timeline）：继续下一动作
    continue;
}
```

- [ ] **Step 5: 跑测试确认通过** —— `./gradlew :plugin:test --tests "*ScriptRunnerTest"`。

- [ ] **Step 6: Commit** —— `0.7.1-P1-T4: ScriptRunner playTimelineAwait 挂起续接 + TRIGGER_DETAIL 透传`，push。

### Task 5: ScriptRuleValidator 6 case + ScriptPermissions 6 case

**Files:**
- Modify: `plugin/src/main/java/moe/hikari/canvas/script/ScriptRuleValidator.java`
- Modify: `plugin/src/main/java/moe/hikari/canvas/script/ScriptPermissions.java`
- Test: `plugin/src/test/java/moe/hikari/canvas/script/ScriptRuleValidatorTest.java`

- [ ] **Step 1: 写失败测试** —— 每个新 action 一组合法/非法用例：
  - setElementProperties：patch 空 → 错；patch 含非白名单键 → 错；patch 键超 8 → 错；合法 `{x,y}` kind=moveTo → 过
  - nudgeElement：elementId 空 → 错；dx NaN → 错；合法 → 过
  - sendMessage：text null → 错；text 超 `MESSAGE_MAX` → 错；channel 非法 → 错；合法 → 过
  - setRandomVariable：fullName 空 → 错；min>max → 错；min NaN → 错；合法 → 过
  - scaleVariable：fullName 空 → 错；op 非法 → 错；factor NaN → 错；divide factor=0 → 错；合法 → 过
  - playTimelineAwait：timelineId 空 → 错；合法 → 过

- [ ] **Step 2: 跑测试确认失败**。

- [ ] **Step 3: ScriptRuleValidator 加常量 + 6 case** —— 常量区加：

```java
public static final int MESSAGE_MAX = 256;
public static final int PATCH_MAX_KEYS = 8;
public static final int KIND_MAX = 32;
public static final Set<String> MESSAGE_CHANNELS = Set.of("chat", "actionbar", "title");
public static final Set<String> SCALE_OPS = Set.of("multiply", "divide");
```

  `validateAction` switch 加（`If` case 之后）：

```java
case Action.SetElementProperties a -> {
    if (blank(a.elementId())) yield Optional.of("设置元素属性缺少元素 ID");
    if (a.patch().isEmpty()) yield Optional.of("批量设属性的 patch 不能为空");
    if (a.patch().size() > PATCH_MAX_KEYS) yield Optional.of("patch 属性数超过 " + PATCH_MAX_KEYS);
    if (a.kind() != null && a.kind().length() > KIND_MAX) yield Optional.of("kind 超长");
    for (Map.Entry<String, String> e : a.patch().entrySet()) {
        if (!ELEMENT_PROPERTIES.contains(e.getKey())) {
            yield Optional.of("元素属性不在允许范围：" + e.getKey());
        }
        if (blank(e.getValue())) yield Optional.of("属性 " + e.getKey() + " 的值不能为空");
    }
    yield Optional.empty();
}
case Action.NudgeElement a -> {
    if (blank(a.elementId())) yield Optional.of("相对移动缺少元素 ID");
    if (!Double.isFinite(a.dx()) || !Double.isFinite(a.dy())) {
        yield Optional.of("相对移动的 dx/dy 必须是有限数值");
    }
    yield Optional.empty();
}
case Action.SendMessage a -> {
    if (a.text() == null) yield Optional.of("发消息内容不能为 null");
    if (a.text().length() > MESSAGE_MAX) yield Optional.of("发消息内容超长（最多 " + MESSAGE_MAX + "）");
    if (a.channel() == null || !MESSAGE_CHANNELS.contains(a.channel())) {
        yield Optional.of("消息渠道不在允许范围：" + a.channel());
    }
    yield Optional.empty();
}
case Action.SetRandomVariable a -> {
    if (blank(a.fullName())) yield Optional.of("随机数变量名不能为空");
    if (!Double.isFinite(a.min()) || !Double.isFinite(a.max())) {
        yield Optional.of("随机区间必须是有限数值");
    }
    if (a.min() > a.max()) yield Optional.of("随机区间 min 不能大于 max");
    yield Optional.empty();
}
case Action.ScaleVariable a -> {
    if (blank(a.fullName())) yield Optional.of("乘除变量名不能为空");
    if (a.op() == null || !SCALE_OPS.contains(a.op())) yield Optional.of("运算不在允许范围：" + a.op());
    if (!Double.isFinite(a.factor())) yield Optional.of("乘除系数必须是有限数值");
    if ("divide".equals(a.op()) && a.factor() == 0.0) yield Optional.of("除数不能为 0");
    yield Optional.empty();
}
case Action.PlayTimelineAwait a ->
    blank(a.timelineId()) ? Optional.of("播时间轴缺少时间轴 ID") : Optional.empty();
```

- [ ] **Step 4: ScriptPermissions.scanActions 加 6 case**（全无附加面，显式列出保穷尽）：

```java
case Action.SetElementProperties ignored -> { }
case Action.NudgeElement ignored -> { }
case Action.SendMessage ignored -> { }
case Action.SetRandomVariable ignored -> { }
case Action.ScaleVariable ignored -> { }
case Action.PlayTimelineAwait ignored -> { }
```

- [ ] **Step 5: 跑测试确认通过** —— `./gradlew :plugin:test --tests "*ScriptRuleValidatorTest" --tests "*ScriptPermissionsTest"`。

- [ ] **Step 6: Commit** —— `0.7.1-P1-T5: 校验 6 case + 权限面 6 case（编译期穷举守门补全）`，push。

### Task 6: 后端全量回归

- [ ] **Step 1:** `./gradlew :plugin:test` 全绿（基线后端 1585 + 新增约 30-40）。
- [ ] **Step 2:** 若有 `EndToEndScriptTest` / `ScriptStoreTest` 因穷举 switch 编译失败，补对应分支。
- [ ] **Step 3: Commit**（若有改动）`0.7.1-P1-T6: 后端全量回归绿`，push。

---

## 批次 3：前端 protocol + 友好积木适配层

### Task 7: protocol.ts ScriptAction union 加 6 成员

**Files:**
- Modify: `web/src/types/protocol.ts`

- [ ] **Step 1:** `ScriptAction` union 加（`if` 成员之后）：

```typescript
    | { type: 'setElementProperties'; elementId: string; patch: Record<string, string>; kind: string }
    | { type: 'nudgeElement'; elementId: string; dx: number; dy: number }
    | { type: 'sendMessage'; text: string; channel: 'chat' | 'actionbar' | 'title' }
    | { type: 'setRandomVariable'; fullName: string; min: number; max: number }
    | { type: 'scaleVariable'; fullName: string; op: 'multiply' | 'divide'; factor: number }
    | { type: 'playTimelineAwait'; timelineId: string };
```

- [ ] **Step 2: Commit** —— `0.7.1-P1-T7: protocol.ts ScriptAction union +6`，push。

### Task 8: blockDefs.ts —— 友好积木定义 + 新动作 blockDef + 默认值

**Files:**
- Modify: `web/src/script/model/blockDefs.ts`
- Test: `web/src/script/model/__tests__/blockDefs.test.ts` / `blockDefaults.test.ts`

- [ ] **Step 1: 写失败测试** —— ① `FRIENDLY_ELEMENT_DEFS` 含 8 个 kind（moveTo/resize/rotateTo/setOpacity/show/hide/setText/setColor），每个有 labelKey + patchKeys；② `makeDefaultAction('moveTo')` 返回 `{type:'setElementProperties', kind:'moveTo', elementId:'', patch:{x:'0',y:'0'}}`；③ `makeDefaultAction('sendMessage')` 返回合法非空默认；④ 新动作 blockDef 存在于 `ACTION_DEFS`（nudgeElement/sendMessage/setRandomVariable/scaleVariable/playTimelineAwait）。

- [ ] **Step 2: 跑测试确认失败** —— `cd web && ./node_modules/.bin/vitest run blockDefs blockDefaults`。

- [ ] **Step 3: 加 FRIENDLY_ELEMENT_DEFS（按 kind 索引）** —— 新结构（友好元素积木的皮肤声明；BlockNode 据 action.kind 查它渲染）：

```typescript
/** 0.7.1：友好元素积木皮肤。kind → 标题 + 该 kind 对应的 patch 字段。
 * BlockNode 渲染 setElementProperties 时按 action.kind 查此表选皮肤；字段值读写 action.patch[name]。
 * fixedPatch 非空 = 无可编辑字段（如 show/hide），拖出即定值。 */
export interface FriendlyElementDef {
    kind: string;
    labelKey: string;
    /** 可编辑字段（name = patch 键）。show/hide 为空数组。 */
    fields: FieldDef[];
    /** 默认 patch（拖出时）。 */
    defaultPatch: Record<string, string>;
}

export const FRIENDLY_ELEMENT_DEFS: Record<string, FriendlyElementDef> = {
    moveTo: {
        kind: 'moveTo', labelKey: 'script.friendly.moveTo',
        fields: [
            { name: 'x', type: 'number', labelKey: 'script.fieldOptions.propX' },
            { name: 'y', type: 'number', labelKey: 'script.fieldOptions.propY' },
        ],
        defaultPatch: { x: '0', y: '0' },
    },
    resize: {
        kind: 'resize', labelKey: 'script.friendly.resize',
        fields: [
            { name: 'w', type: 'number', labelKey: 'script.fieldOptions.propW', min: 1 },
            { name: 'h', type: 'number', labelKey: 'script.fieldOptions.propH', min: 1 },
        ],
        defaultPatch: { w: '64', h: '64' },
    },
    rotateTo: {
        kind: 'rotateTo', labelKey: 'script.friendly.rotateTo',
        fields: [{ name: 'rotation', type: 'number', labelKey: 'script.fieldOptions.propRotation', min: 0, max: 359 }],
        defaultPatch: { rotation: '0' },
    },
    setOpacity: {
        kind: 'setOpacity', labelKey: 'script.friendly.setOpacity',
        fields: [{ name: 'opacity', type: 'number', labelKey: 'script.fieldOptions.propOpacity', min: 0, max: 1, step: 0.1 }],
        defaultPatch: { opacity: '1' },
    },
    show: {
        kind: 'show', labelKey: 'script.friendly.show',
        fields: [], defaultPatch: { opacity: '1' },
    },
    hide: {
        kind: 'hide', labelKey: 'script.friendly.hide',
        fields: [], defaultPatch: { opacity: '0' },
    },
    setText: {
        kind: 'setText', labelKey: 'script.friendly.setText',
        fields: [{ name: 'text', type: 'text', labelKey: 'script.fieldOptions.propText' }],
        defaultPatch: { text: '' },
    },
    setColor: {
        kind: 'setColor', labelKey: 'script.friendly.setColor',
        fields: [{ name: 'fill', type: 'text', labelKey: 'script.fieldOptions.propFill' }],
        defaultPatch: { fill: '#FFFFFF' },
    },
};
```

  注：`setColor` 用 `text` 字段输 hex（P1 从简，颜色选择器留后续）；`setText` 用 `text` 字段（支持手打 `${var:X}`）。

- [ ] **Step 4: makeDefaultAction 加 friendly + 新动作 case** —— 在现有 switch 加：

```typescript
// 友好元素积木：kind 在 FRIENDLY_ELEMENT_DEFS 里 → setElementProperties
case 'moveTo': case 'resize': case 'rotateTo': case 'setOpacity':
case 'show': case 'hide': case 'setText': case 'setColor': {
    const def = FRIENDLY_ELEMENT_DEFS[kind];
    return { type: 'setElementProperties', elementId: '', patch: { ...def.defaultPatch }, kind };
}
case 'nudgeElement':
    return { type: 'nudgeElement', elementId: '', dx: 0, dy: 0 };
case 'sendMessage':
    return { type: 'sendMessage', text: '', channel: 'chat' };
case 'setRandomVariable':
    return { type: 'setRandomVariable', fullName: 'user/roll', min: 1, max: 6 };
case 'scaleVariable':
    return { type: 'scaleVariable', fullName: 'user/score', op: 'multiply', factor: 2 };
case 'playTimelineAwait':
    return { type: 'playTimelineAwait', timelineId: '' };
```

  （`sendMessage` text 空串合法——后端 text=="" 合法，非引用字段，不触发「待完善」角标，照 0.7.0 log 范式。）

- [ ] **Step 5: ACTION_DEFS 加 5 个新动作 blockDef**（nudge + 4 新动作；friendly 元素积木**不**进 ACTION_DEFS，它们走 FRIENDLY_ELEMENT_DEFS + palette 单列，见 Task 9/11）：

```typescript
nudgeElement: {
    kind: 'nudgeElement', category: 'action', colorVar: CATEGORY_COLOR_VAR.action,
    labelKey: 'script.blocks.nudgeElement',
    fields: [
        { name: 'elementId', type: 'element', labelKey: 'script.fields.elementId' },
        { name: 'dx', type: 'number', labelKey: 'script.fields.dx', step: 1 },
        { name: 'dy', type: 'number', labelKey: 'script.fields.dy', step: 1 },
    ],
},
sendMessage: {
    kind: 'sendMessage', category: 'action', colorVar: CATEGORY_COLOR_VAR.action,
    labelKey: 'script.blocks.sendMessage',
    fields: [
        { name: 'text', type: 'text', labelKey: 'script.fields.messageText' },
        { name: 'channel', type: 'select', labelKey: 'script.fields.channel', options: MESSAGE_CHANNEL_OPTIONS },
    ],
},
setRandomVariable: {
    kind: 'setRandomVariable', category: 'action', colorVar: CATEGORY_COLOR_VAR.action,
    labelKey: 'script.blocks.setRandomVariable',
    fields: [
        { name: 'fullName', type: 'variable', labelKey: 'script.fields.fullName' },
        { name: 'min', type: 'number', labelKey: 'script.fields.min', step: 1 },
        { name: 'max', type: 'number', labelKey: 'script.fields.max', step: 1 },
    ],
},
scaleVariable: {
    kind: 'scaleVariable', category: 'action', colorVar: CATEGORY_COLOR_VAR.action,
    labelKey: 'script.blocks.scaleVariable',
    fields: [
        { name: 'fullName', type: 'variable', labelKey: 'script.fields.fullName' },
        { name: 'op', type: 'select', labelKey: 'script.fields.scaleOp', options: SCALE_OP_OPTIONS },
        { name: 'factor', type: 'number', labelKey: 'script.fields.factor', step: 1 },
    ],
},
playTimelineAwait: {
    kind: 'playTimelineAwait', category: 'timeline', colorVar: CATEGORY_COLOR_VAR.timeline,
    labelKey: 'script.blocks.playTimelineAwait',
    fields: [{ name: 'timelineId', type: 'timeline', labelKey: 'script.fields.timelineId' }],
},
```

  加选项常量（照 `ELEMENT_PROPERTY_OPTIONS` 范式）：

```typescript
const MESSAGE_CHANNEL_OPTIONS: FieldOption[] = [
    { value: 'chat', labelKey: 'script.fieldOptions.channelChat' },
    { value: 'actionbar', labelKey: 'script.fieldOptions.channelActionbar' },
    { value: 'title', labelKey: 'script.fieldOptions.channelTitle' },
];
const SCALE_OP_OPTIONS: FieldOption[] = [
    { value: 'multiply', labelKey: 'script.fieldOptions.opMultiply' },
    { value: 'divide', labelKey: 'script.fieldOptions.opDivide' },
];
```

- [ ] **Step 6: 跑测试确认通过**。

- [ ] **Step 7: Commit** —— `0.7.1-P1-T8: blockDefs 友好积木定义 + 5 新动作 blockDef + 默认值`，push。

### Task 9: BlockNode 友好皮肤渲染 + palette 友好积木列

**Files:**
- Modify: `web/src/script/canvas/BlockNode.vue`（实现者先读现状：blockDef.fields 驱动渲染 + BlockParamInput + 待完善角标）
- Modify: `web/src/script/canvas/BlockPalette.vue`（加「友好元素」分组）
- Modify: `web/src/script/model/blockDefs.ts`（palette 友好积木列表 + category）
- Test: `web/src/script/canvas/__tests__/BlockNode.smoke.test.ts`

- [ ] **Step 1: 写失败测试** —— ① mount `BlockNode` with `{type:'setElementProperties', kind:'moveTo', elementId:'e-1', patch:{x:'128',y:'64'}}` → 渲染含「移到」标题 + 两个 number input 值 128/64 + `data-block-path` 不变；② 改 x input → `edit.updateActionField(path, {patch:{x:'NEW',y:'64'}})` 被调（patch 整体替换）；③ kind 未知 → fallback 通用「批量设属性」不崩；④ palette 含「友好元素」分组 8 项。

- [ ] **Step 2: 跑测试确认失败**。

- [ ] **Step 3: BlockNode 加 friendly 渲染分支** —— 在现有「按 blockDef.fields 渲染」逻辑前加判断：当 `action.type === 'setElementProperties'`，用 `FRIENDLY_ELEMENT_DEFS[action.kind]` 取皮肤：
  - 标题用 `friendlyDef.labelKey`（未知 kind → fallback `script.blocks.setElementProperties` 通用名）
  - 字段渲染 `friendlyDef.fields`，每个字段值 = `action.patch[field.name]`，`onChange(v)` → `edit.updateActionField(path, { patch: { ...action.patch, [field.name]: String(v) } })`
  - `elementId` 字段（element 下拉）单列在 patch 字段之前（friendly 积木都先选元素）
  - show/hide（fields 为空）→ 只渲染元素下拉 + 标题
  - 「待完善」角标：elementId 空 → 显示（复用现有角标逻辑，friendly 积木的必填引用字段是 elementId）

  关键：friendly 渲染产出的 `data-block-path` 仍是 `props.path`（1 积木 1 path，同构不破）。

- [ ] **Step 4: blockDefs 加 palette 友好积木列表** —— palette 需要把 8 个 friendly kind 作为可拖项。加导出：

```typescript
/** palette「友好元素」分组的可拖项（kind 即拖出标识，makeDefaultAction(kind) 生成 action）。 */
export const FRIENDLY_PALETTE_KINDS: string[] =
    ['moveTo', 'nudgeElement', 'resize', 'rotateTo', 'setOpacity', 'show', 'hide', 'setText', 'setColor'];
```

  （`nudgeElement` 混在友好元素分组里——它是元素操作，对用户是「移动一点」，虽走独立 action。）

- [ ] **Step 5: BlockPalette 加「友好元素」分组渲染** —— 现有 palette 按 `ACTION_DEFS` 的 category 分组。加一个「友好元素」分组，遍历 `FRIENDLY_PALETTE_KINDS`：每项 label 用 friendly labelKey（nudgeElement 用 `script.blocks.nudgeElement`），点击/拖拽走与现有积木相同的 `makeDefaultAction(kind)` 路径。实现者照现有 palette 项的拖拽 wiring 复制（拖出 payload = kind，落画布 insert `makeDefaultAction(kind)`）。

- [ ] **Step 6: 跑测试确认通过**。

- [ ] **Step 7: Commit** —— `0.7.1-P1-T9: BlockNode 友好皮肤渲染 + palette 友好元素分组`，push。

### Task 10: validator.ts 前端镜像 6 case

**Files:**
- Modify: `web/src/script/model/validator.ts`
- Test: `web/src/script/model/__tests__/validator.test.ts`

- [ ] **Step 1: 写失败测试** —— 镜像后端 Task 5 的用例（合法/非法各一组，6 个新 action）。错误文案与后端逐字一致（照现有 validator.ts 镜像纪律）。

- [ ] **Step 2: 跑测试确认失败**。

- [ ] **Step 3: 加常量 + 6 case** —— 常量区：

```typescript
export const MESSAGE_MAX = 256;
export const PATCH_MAX_KEYS = 8;
export const KIND_MAX = 32;
export const MESSAGE_CHANNELS = new Set(['chat', 'actionbar', 'title']);
export const SCALE_OPS = new Set(['multiply', 'divide']);
```

  `validateAction` switch 加 6 case（文案与后端逐字一致）：

```typescript
case 'setElementProperties': {
    if (isBlank(action.elementId)) errors.push({ blockId: path, message: '设置元素属性缺少元素 ID' });
    const keys = Object.keys(action.patch ?? {});
    if (keys.length === 0) errors.push({ blockId: path, message: '批量设属性的 patch 不能为空' });
    if (keys.length > PATCH_MAX_KEYS) errors.push({ blockId: path, message: `patch 属性数超过 ${PATCH_MAX_KEYS}` });
    if (action.kind != null && action.kind.length > KIND_MAX) errors.push({ blockId: path, message: 'kind 超长' });
    for (const [k, v] of Object.entries(action.patch ?? {})) {
        if (!ELEMENT_PROPERTIES.has(k)) errors.push({ blockId: path, message: `元素属性不在允许范围：${k}` });
        if (isBlank(v)) errors.push({ blockId: path, message: `属性 ${k} 的值不能为空` });
    }
    break;
}
case 'nudgeElement': {
    if (isBlank(action.elementId)) errors.push({ blockId: path, message: '相对移动缺少元素 ID' });
    if (!Number.isFinite(action.dx) || !Number.isFinite(action.dy)) {
        errors.push({ blockId: path, message: '相对移动的 dx/dy 必须是有限数值' });
    }
    break;
}
case 'sendMessage': {
    if (action.text == null) errors.push({ blockId: path, message: '发消息内容不能为 null' });
    else if (action.text.length > MESSAGE_MAX) errors.push({ blockId: path, message: `发消息内容超长（最多 ${MESSAGE_MAX}）` });
    if (action.channel == null || !MESSAGE_CHANNELS.has(action.channel)) {
        errors.push({ blockId: path, message: `消息渠道不在允许范围：${action.channel}` });
    }
    break;
}
case 'setRandomVariable': {
    if (isBlank(action.fullName)) errors.push({ blockId: path, message: '随机数变量名不能为空' });
    if (!Number.isFinite(action.min) || !Number.isFinite(action.max)) {
        errors.push({ blockId: path, message: '随机区间必须是有限数值' });
    } else if (action.min > action.max) errors.push({ blockId: path, message: '随机区间 min 不能大于 max' });
    break;
}
case 'scaleVariable': {
    if (isBlank(action.fullName)) errors.push({ blockId: path, message: '乘除变量名不能为空' });
    if (action.op == null || !SCALE_OPS.has(action.op)) errors.push({ blockId: path, message: `运算不在允许范围：${action.op}` });
    if (!Number.isFinite(action.factor)) errors.push({ blockId: path, message: '乘除系数必须是有限数值' });
    else if (action.op === 'divide' && action.factor === 0) errors.push({ blockId: path, message: '除数不能为 0' });
    break;
}
case 'playTimelineAwait': {
    if (isBlank(action.timelineId)) errors.push({ blockId: path, message: '播时间轴缺少时间轴 ID' });
    break;
}
```

- [ ] **Step 4: 跑测试确认通过**。

- [ ] **Step 5: Commit** —— `0.7.1-P1-T10: validator.ts 前端镜像 6 case`，push。

---

## 批次 4：i18n + 收尾

### Task 11: i18n 中英文案

**Files:**
- Modify: `web/src/i18n/messages.ts`

- [ ] **Step 1:** 中英两份各加：
  - `script.blocks.{nudgeElement, sendMessage, setRandomVariable, scaleVariable, playTimelineAwait}`：相对移动 / 发消息 / 设随机数 / 变量乘除 / 播放并等待
  - `script.friendly.{moveTo, resize, rotateTo, setOpacity, show, hide, setText, setColor}`：移到 / 改大小 / 旋转到 / 设透明度 / 显示 / 隐藏 / 改文字 / 改颜色
  - `script.fields.{dx, dy, messageText, channel, min, max, scaleOp, factor}`：横向移动 / 纵向移动 / 消息内容 / 发到哪 / 最小 / 最大 / 运算 / 倍数
  - `script.fieldOptions.{channelChat, channelActionbar, channelTitle, opMultiply, opDivide}`：聊天框 / 动作栏 / 标题 / 乘以 / 除以
  - `script.paletteGroup.friendly`：元素动作（BlockPalette 友好分组标题）
  - 大白话，无内部术语（照 feedback_plain_language）

- [ ] **Step 2:** `cd web && ./node_modules/.bin/vite build --clearScreen false` 编译通过（无缺 key 类型错）。

- [ ] **Step 3: Commit** —— `0.7.1-P1-T11: i18n 中英文案（友好积木 + 新动作）`，push。

### Task 12: 前端全量回归 + 友好积木集成 smoke

**Files:**
- Test: `web/src/script/canvas/__tests__/BlockCanvas.smoke.test.ts`（补一条 friendly 积木拖入即显示 + 改值不丢的 smoke）

- [ ] **Step 1:** 加 smoke：palette 拖「移到」→ workingCopy.actions 多一条 `setElementProperties{kind:moveTo}` → 画布渲染含「移到」标题（验证 0.7.0「拖即显示」根因修复在 friendly 路径也成立）。
- [ ] **Step 2:** `cd web && pkill -f vitest; ./node_modules/.bin/vitest run`（单进程，避免 exit 144），全绿（基线 917 + 新增约 25-35）。
- [ ] **Step 3:** `./node_modules/.bin/vite build --clearScreen false` 出 `script-engine` chunk 正常。
- [ ] **Step 4: Commit** —— `0.7.1-P1-T12: 前端全量回归绿 + friendly 拖入 smoke`，push。

### Task 13: 端到端验证 + 文档 + 版本收尾

**Files:**
- Modify: `docs/journal.md`（顶部加 0.7.1-P1 条）
- Modify: `docs/scripting-0.7.1.md`（§8 P1 标 ✅；§9 勾掉 nudge / 显隐两项——已在启动时勾）

- [ ] **Step 1:** `./gradlew :plugin:shadowJar` 出 `HikariCanvas-0.7.0-SNAPSHOT.jar`（P1 不升版本号，仍 0.7.0；版本号升 0.7.1-SNAPSHOT 留 P5 收尾，与 0.7.0 各 phase 不单独升版一致）。确认 jar 大小正常。
- [ ] **Step 2:** journal 顶部追加：日期 2026-06-11 · 范围 0.7.1-P1 · 改动（6 新 Action + friendly 积木适配层 + 4 新动作）· 关联文件。
- [ ] **Step 3:** scripting-0.7.1.md §8 表 P1 行标 ✅。
- [ ] **Step 4: Commit** —— `0.7.1-P1 完工: 友好积木 + 低风险新动作（6 Action 子类 + friendly 适配层 + nudge/发消息/随机/乘除/播完等待）`，push。
- [ ] **Step 5:** `gh api /repos/HyacinthHaru/HikariCanvas/commits/<sha> --jq '.commit.verification.verified'` == `true`。

---

## 测试基线

- 后端：1585 → 约 1620（+30-40：wire 6 + applier 3 + executor 8 + runner 3 + validator 12 + permissions）
- 前端：917 → 约 950（+25-35：blockDefs/defaults + validator 12 + BlockNode friendly + palette + canvas smoke）
- 0 snapshot baseline 漂移（不碰渲染管线）

## 实测闸（P1 末，交付用户）

游戏内：拖「移到」积木选元素填 x/y → 触发 → 元素移到那；「相对移动」+dx/dy → 元素挪一段；「发消息」→ 触发玩家收到消息（chat/actionbar/title）；「设随机数」「变量乘除」→ 变量按预期变；「播放并等待」→ 时间轴播完后才执行下一积木。

## 自检（writing-plans self-review）

- **spec 覆盖**：§3 友好积木（8 个 → Task 8/9）✓；§4 新动作（发消息/随机/乘除/播完等待 + nudge → Task 1/3/5）✓；§3 显隐用 opacity（show/hide kind defaultPatch opacity 1/0）✓；nudge 走后端（Task 2）✓。粒子/等待直到/停止脚本 = P5 范围，本计划不含（符合 §8）✓。
- **类型一致**：后端 `SetElementProperties(elementId, patch, kind)` ↔ 前端 `{elementId, patch, kind}` ✓；`scaleVariable(fullName, op, factor)` 双端一致 ✓；`nudgeElement(elementId, dx, dy)` 双端 ✓。
- **占位符**：无 TBD；friendly 渲染/palette wiring 标注「实现者读现状适配」是因 BlockNode.vue/BlockPalette.vue 是现有复杂组件，给设计 + 关键代码骨架 + 测试断言，符合集成 task 范式。
- **协议版本**：P1 不升（仍 v4/0.7.0），明确记于 Task 13 Step 1。
