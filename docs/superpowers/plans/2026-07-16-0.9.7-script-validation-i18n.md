# 0.9.7 脚本校验报错 i18n Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把编辑器保存脚本时的 ~100 条校验报错（目前硬编码中文）国际化——1.0 前最后一处 i18n 缺口。按**编辑器 UI 语言**显示。

**Architecture:** 校验报错经 WS `Envelope.error(id, "SCRIPT_INVALID", <message>)` 发编辑器，前端把 `<message>` 原样显示在"新建规则失败：…"横幅。故**后端按编辑器 locale 渲染好这句字符串**即可，前端几乎不改。做法：`ScriptRuleValidator.validate` / `ScriptOpDispatcher.checkConditionSyntax` 从"返回中文字符串"改为"返回 `ValidationError`(key + 参数)"；`ScriptOpDispatcher` 用既有 `Messages`（0.8.2 引入，支持 MiniMessage + 命名占位符 + 指定 locale）按 `session.editorLocale` 渲染；~100 个 key 进 `lang/{en_us,zh_cn}.yml`。前端只需 auth 帧带上 `ui.locale`，后端存进 session。

**Tech Stack:** Java 21 / 既有 `i18n/Messages` + `lang/*.yml` / `LangFileParityTest` 防漏译 / 前端 TS（auth payload）。

## Global Constraints

- **i18n 分流**：玩家/编辑器可见文字走 `lang/{en_us,zh_cn}.yml`（`LangFileParityTest` 防漏译）；控制台日志英文。本批把 ~100 条校验报错纳入 lang yml。
- **用户面向文案大白话**：报错给玩家看，禁内部编号；en 译文要地道、不机翻腔。
- **每任务跑全量 `:plugin:test`**（含 `LangFileParityTest` / `MessagesTest`）；前端任务跑 `cd web && npm run test`。
- **提交规范**：SSH 签名，身份 `HaruHyacinth`，**禁 Claude 署名**；**禁 `git add -A`**（工作区有未跟踪 scratch findings/progress/task_plan.md，只 add 自己任务文件）。SSH 本地 `%G?`=N 是环境所致，正常 commit。
- **版本串**：当前 `0.9.6-SNAPSHOT`，到收尾（Task 4）才 bump 到 `0.9.7-SNAPSHOT`。
- **子代理一律用 opus**。

---

## File Structure

| 文件 | 任务 | 责任 |
|---|---|---|
| `plugin/.../script/ValidationError.java`（新） | T1 | 校验错误值对象（key + 参数 map） |
| `plugin/.../i18n/Messages.java` | T1 | 加 `plain(localeId, key, resolvers)` —— Component→纯文本 String（WS message 用） |
| `plugin/.../session/Session.java` | T1 | 加 `editorLocale` 字段 + setter |
| `web/src/network/wsClient.ts` | T1 | auth payload 带 `locale: ui.locale` |
| `plugin/.../web/WebServer.java` | T1 | auth 读 `locale` → `session.setEditorLocale(messages.resolveLocaleId(...))` |
| `plugin/.../web/ScriptOpDispatcher.java` | T1 注入 / T2·T3 渲染 | 注入 `Messages`；validate/condition 错误按 `session.editorLocale` 渲染 |
| `plugin/.../script/ScriptRuleValidator.java` | T2 | `validate` → `Optional<ValidationError>`（~94 站点） |
| `plugin/.../web/ScriptOpDispatcher.java`（checkConditionSyntax） | T3 | `checkConditionSyntax` → `Optional<ValidationError>`（~8 站点） |
| `plugin/src/main/resources/lang/{en_us,zh_cn}.yml` | T2·T3 | ~100 key（`script.validate.*`） |
| 测试 + 版本 + journal | T4 | 收尾 |

---

### Task 1: 基建（ValidationError + Messages.plain + editorLocale 端到端 + Messages 注入）

**Files:**
- Create: `plugin/src/main/java/moe/hikari/canvas/script/ValidationError.java`
- Modify: `plugin/.../i18n/Messages.java` / `session/Session.java` / `web/WebServer.java` / `web/ScriptOpDispatcher.java`
- Modify: `web/src/network/wsClient.ts`
- Test: `plugin/.../i18n/MessagesTest.java`（加 plain() 用例）；前端 auth-locale 由 T4 vitest 覆盖

**Interfaces:**
- Produces: `ValidationError`（record）；`Messages.plain(String localeId, String key, TagResolver... resolvers)`；`Session.editorLocale()` / `setEditorLocale(String)`；ScriptOpDispatcher 持 `Messages messages`。
- 本任务**不改** validate/checkConditionSyntax 的返回类型（仍 String）——只铺基建，让 T2/T3 能落地。auth locale 端到端打通。

- [ ] **Step 1: `ValidationError` 值对象**

```java
package moe.hikari.canvas.script;

import java.util.LinkedHashMap;
import java.util.Map;

/**
 * 0.9.7：脚本校验错误的结构化载体，取代原先"返回中文字符串"。{@code key} 是 lang yml 里
 * {@code script.validate.<key>} 的后缀；{@code params} 是命名占位符（如 {@code max=64}）。
 * 由 {@code ScriptOpDispatcher} 用 {@code Messages} 按编辑器 locale 渲染成最终文案。
 */
public record ValidationError(String key, Map<String, String> params) {

    /** 无参报错。 */
    public static ValidationError of(String key) {
        return new ValidationError(key, Map.of());
    }

    /** 带命名参数的报错。{@code kv} 必须成对：key1, val1, key2, val2 …（val 用 String.valueOf 转）。 */
    public static ValidationError of(String key, Object... kv) {
        if (kv.length % 2 != 0) {
            throw new IllegalArgumentException("params must be key-value pairs");
        }
        Map<String, String> m = new LinkedHashMap<>();
        for (int i = 0; i < kv.length; i += 2) {
            m.put(String.valueOf(kv[i]), String.valueOf(kv[i + 1]));
        }
        return new ValidationError(key, m);
    }
}
```

- [ ] **Step 2: `Messages.plain()` —— 渲染成纯文本 String**

在 `Messages` 里 `get(...)` 方法附近加（WS message 字段是 String，不是 Component；validation 文案无格式，纯文本即可）：

```java
    /**
     * 0.9.7：渲染成纯文本 String（WS error message 字段用）。{@link #get} 出 Component 后
     * 用 PlainTextComponentSerializer 抽纯文本；找不到 key 时 get() 已回退 defaultLocale / key 名。
     */
    public String plain(String localeId, String key, TagResolver... resolvers) {
        return net.kyori.adventure.text.serializer.plain.PlainTextComponentSerializer
                .plainText().serialize(get(localeId, key, resolvers));
    }
```

> 若 `PlainTextComponentSerializer` 不在 classpath，它随 adventure-api 提供（MiniMessage 已在用，同 jar 家族）；编译报缺失时确认 adventure 依赖已含 `net.kyori.adventure.text.serializer.plain`。

- [ ] **Step 3: `Session` 加 `editorLocale`**

字段区（`private volatile String boundIp;` 附近）加：

```java
    /** 0.9.7：编辑器 UI 语言（前端 auth 帧携带 ui.locale）。用于按编辑器语言渲染脚本校验报错。
     *  null = 未知，渲染时回退 Messages 默认 locale。 */
    private volatile String editorLocale;
```

访问器（`playerName()` 附近）：

```java
    public String editorLocale() { return editorLocale; }
    public void setEditorLocale(String localeId) { this.editorLocale = localeId; }
```

- [ ] **Step 4: 前端 auth 帧带 `locale`**

在 `wsClient.ts` 组 auth payload 处（现有 `client_v` 那个 payload）加一个字段 `locale: useUiStore().locale`。找到发 auth 的地方（`op: 'auth'` / `client_v`），把 payload 从 `{ token, client_v }` 形态扩成 `{ token, client_v, locale: <ui.locale> }`。实现者按 wsClient 真实的 auth 组装代码就地加（ui store 已在文件内可用，见 `localizeErrorCode` 里 `useUiStore()`）。

- [ ] **Step 5: 后端 auth 读 `locale` 存 session**

在 `WebServer` 的 auth 处理里（读到 `client_v` / 建/取 Session 之后），读 payload 的 `locale` 字段 → `session.setEditorLocale(messages.resolveLocaleId(rawLocale))`（`resolveLocaleId` 已存在，规范化 + 兜底）。`rawLocale` 缺失时不设（editorLocale 保持 null，渲染回退默认 locale）。实现者定位 auth handler 里 Session 就绪的点接线；`messages` 引用若 WebServer 未持有，从既有装配取（WebServer 构造已注入不少依赖，Messages 应可获得——若未注入，本步补注入 Messages 到 WebServer + 传给 ScriptOpDispatcher，见 Step 6）。

- [ ] **Step 6: `ScriptOpDispatcher` 注入 `Messages`**

给 ScriptOpDispatcher 构造加 `Messages messages` 参数 + 字段（放依赖列表里）；`WebServer` 构造 ScriptOpDispatcher 处把 messages 传入。本步**只注入不使用**（T2/T3 才用）。

- [ ] **Step 7: `MessagesTest` 加 plain() 用例**

```java
    @Test
    void plain_rendersNamedParam_asPlainText() {
        Messages m = new Messages(java.util.logging.Logger.getLogger("t"));
        m.loadBuiltIn();
        // 用一个已存在的带占位符的 key 验证 plain() 出纯文本（无 MiniMessage 标签残留）；
        // 实现者挑 lang yml 里任一带 <占位> 的现成 key，或临时 putLocale 注入测试 key。
        String s = m.plain("en_us", "<某个现成 key>", net.kyori.adventure.text.minimessage.tag.resolver.Placeholder.unparsed("x", "42"));
        org.junit.jupiter.api.Assertions.assertFalse(s.contains("<"), "不应残留 MiniMessage 标签: " + s);
    }
```

> 实现者按 `MessagesTest` 现有风格挑一个现成带占位符的 key 验证；核心是 plain() 出纯文本、参数被填充。

- [ ] **Step 8: 全量 + 前端测试**

Run: `./gradlew :plugin:test --no-daemon` + `cd web && npm run test`
Expected: 双绿；基建就位，validate 仍返 String（行为未变）。

- [ ] **Step 9: Commit**

```bash
git add plugin/src/main/java/moe/hikari/canvas/script/ValidationError.java \
        plugin/src/main/java/moe/hikari/canvas/i18n/Messages.java \
        plugin/src/main/java/moe/hikari/canvas/session/Session.java \
        plugin/src/main/java/moe/hikari/canvas/web/WebServer.java \
        plugin/src/main/java/moe/hikari/canvas/web/ScriptOpDispatcher.java \
        plugin/src/test/java/moe/hikari/canvas/i18n/MessagesTest.java \
        web/src/network/wsClient.ts
git commit -m "feat(i18n): 脚本校验 i18n 基建（ValidationError + Messages.plain + editorLocale 端到端）"
```

---

### Task 2: `ScriptRuleValidator` → `ValidationError` + 渲染 + lang keys（大头）

**Files:**
- Modify: `plugin/.../script/ScriptRuleValidator.java`（`validate` 及其所有私有子校验 → `Optional<ValidationError>`）
- Modify: `plugin/.../web/ScriptOpDispatcher.java`（validate 两处调用点渲染）
- Modify: `plugin/src/main/resources/lang/en_us.yml` + `lang/zh_cn.yml`（validator 的 ~94 key）

**Interfaces:**
- Consumes: T1 的 `ValidationError` / `Messages.plain` / `Session.editorLocale`。

**转换规则（对全部 ~94 站点机械套用）：**
- `Optional.of("中文串")` → `Optional.of(ValidationError.of("<camelKey>"))`
- 带参 `Optional.of("… " + X + " …")` → `Optional.of(ValidationError.of("<camelKey>", "<p名>", X))`
- key 命名：语义化 camelCase（如 `ruleNull` / `nameTooLong` / `timerRange` / `nearRange`）；lang yml 里挂在 `script.validate.<camelKey>`
- 私有子校验（`validateTrigger` / `validateActions` / `validateAction` 等）返回类型一并从 `Optional<String>` 改 `Optional<ValidationError>`；`validate` 顶层签名 `public static Optional<ValidationError> validate(ScriptRule rule)`

- [ ] **Step 1: 逐站点转换 `ScriptRuleValidator`**（示范 4 型，其余照此机械做）

```java
// 无参
return Optional.of(ValidationError.of("ruleNull"));                 // 原 "规则不能为空"
// 带一参
return Optional.of(ValidationError.of("nameTooLong", "max", NAME_MAX));   // 原 "规则名称超长（最多 " + NAME_MAX + " 字符）"
// 带两参（范围）
return Optional.of(ValidationError.of("timerRange", "min", TIMER_MIN, "max", TIMER_MAX)); // 原 "定时器间隔需在 …"
// ternary / switch yield 同理，把 Optional.of("…") 换成 Optional.of(ValidationError.of(...))
```

把 `validate` 及所有相关私有方法的 `Optional<String>` 全改 `Optional<ValidationError>`（编译器会把漏改的站点标出来——全绿即无遗漏）。

- [ ] **Step 2: `ScriptOpDispatcher` 渲染 validate 错误**

两处 `Optional<String> invalid = ScriptRuleValidator.validate(...)`（约 191 / 236 行）改为：

```java
        Optional<ValidationError> invalid = ScriptRuleValidator.validate(parsed.rule());
        if (invalid.isPresent()) {
            return Envelope.error(in.id(), "SCRIPT_INVALID", renderValidation(sessionId, invalid.get()));
        }
```

加私有助手（editorLocale 从 session 取，缺则 null → Messages 回退默认 locale）：

```java
    private String renderValidation(String sessionId, ValidationError ve) {
        Session s = sessionManager.byId(sessionId);
        String locale = (s == null) ? null : s.editorLocale();
        TagResolver[] resolvers = ve.params().entrySet().stream()
                .map(e -> Placeholder.unparsed(e.getKey(), e.getValue()))
                .toArray(TagResolver[]::new);
        return messages.plain(locale, "script.validate." + ve.key(), resolvers);
    }
```

> import：`net.kyori.adventure.text.minimessage.tag.resolver.{TagResolver,Placeholder}`。`renderValidation` 也给 T3 的 condition 错误复用。

- [ ] **Step 3: lang yml 加 validator 的 ~94 key**

`zh_cn.yml`：`script.validate.<key>` = 原中文（占位符 `" + X + "` 改成 MiniMessage 命名占位 `<p名>`，如 `nameTooLong: "规则名称超长（最多 <max> 字符）"`）。
`en_us.yml`：同 key 的地道英文译文（`nameTooLong: "Rule name too long (max <max> characters)"`）。
两文件 key 集必须一致（`LangFileParityTest` 会验）。

- [ ] **Step 4: 全量测试**

Run: `./gradlew :plugin:test --no-daemon`
Expected: BUILD SUCCESSFUL；`LangFileParityTest` 绿（zh/en key 对齐）；既有 script 相关测试若断言过旧中文串，按新渲染/key 校正（见 T4）。

- [ ] **Step 5: Commit**

```bash
git add plugin/src/main/java/moe/hikari/canvas/script/ScriptRuleValidator.java \
        plugin/src/main/java/moe/hikari/canvas/web/ScriptOpDispatcher.java \
        plugin/src/main/resources/lang/en_us.yml plugin/src/main/resources/lang/zh_cn.yml
git commit -m "feat(i18n): ScriptRuleValidator 校验报错 i18n（~94 条 → key+参数 + 双语 lang）"
```

---

### Task 3: `checkConditionSyntax` → `ValidationError` + 渲染 + 其 lang keys

**Files:**
- Modify: `plugin/.../web/ScriptOpDispatcher.java`（`checkConditionSyntax` 递归方法 → `Optional<ValidationError>`；两处调用点渲染）
- Modify: `lang/{en_us,zh_cn}.yml`（condition 的 ~8 key）

- [ ] **Step 1: `checkConditionSyntax` 转 `ValidationError`**

`checkConditionSyntax`（约 434-470 行，递归）的 `Optional<String>` → `Optional<ValidationError>`；内部 `Optional.of("if 条件语法错误（" + blockId + "）: " + err.get())` 这类改为带 `blockId` + 子错误串参数的 `ValidationError.of("conditionSyntaxIf", "blockId", blockId, "detail", <subDetail>)`。子错误 `err` 现在也是 ValidationError——嵌套时先把子错误渲染成串再作 `detail` 参，或把 detail 设计成透传的原始文法错误串（condition 文法错误 `err` 来自 ConditionParser，可能本身是英文/符号，实现者据实处理：若 ConditionParser 的错误是中文也一并纳入 key，若已是英文/符号则作 `detail` 原样透传）。

- [ ] **Step 2: 两处调用点渲染**

`badCondition`（约 196 / 241 行）：`Optional<ValidationError> badCondition = checkConditionSyntax(...)` → `Envelope.error(in.id(), "SCRIPT_INVALID", renderValidation(sessionId, badCondition.get()))`（复用 T2 的 `renderValidation`）。

- [ ] **Step 3: lang yml 加 condition 的 ~8 key** —— `script.validate.conditionSyntax*`，zh + en 对齐。

- [ ] **Step 4: 全量测试**

Run: `./gradlew :plugin:test --no-daemon`
Expected: BUILD SUCCESSFUL + LangFileParityTest 绿。

- [ ] **Step 5: Commit**

```bash
git add plugin/src/main/java/moe/hikari/canvas/web/ScriptOpDispatcher.java \
        plugin/src/main/resources/lang/en_us.yml plugin/src/main/resources/lang/zh_cn.yml
git commit -m "feat(i18n): checkConditionSyntax 条件语法报错 i18n（key+参数 + 双语 lang）"
```

---

### Task 4: 测试收尾 + 收尾（版本 + journal）

**Files:**
- Modify/Create: `plugin/.../script/ScriptRuleValidatorTest.java`（若存在则改断言，否则新建代表性用例）
- Modify: 若有前端脚本相关 vitest 断言旧中文串 → 校正
- Modify: 版本串 6 文件 + `docs/journal.md` + 本 plan

- [ ] **Step 1: `ScriptRuleValidator` 单测校正/补**

`validate` 返回类型变了，既有断言（若断言 `Optional<String>` 内容）改为断言 `ValidationError.key()` + `params()`。若无既有单测，补代表性用例：空规则→`ruleNull` / 名超长→`nameTooLong`+`max` / 定时器越界→`timerRange`。

- [ ] **Step 2: 全量回归（后端 + 前端）**

Run: `./gradlew :plugin:test --no-daemon` + `cd web && npm run test`
Expected: 双绿（含 `LangFileParityTest` / `MessagesTest` / `ScriptRuleValidatorTest`）。

- [ ] **Step 3: 版本 bump `0.9.6 → 0.9.7-SNAPSHOT`**

```bash
perl -i -pe 's/\Q0.9.6-SNAPSHOT\E/0.9.7-SNAPSHOT/g' \
  build.gradle.kts plugin/src/main/resources/paper-plugin.yml \
  examples/demo-train-plugin/src/main/resources/paper-plugin.yml \
  examples/demo-score-plugin/src/main/resources/paper-plugin.yml \
  web/package.json web/package-lock.json
```
Run: `grep -rn "0.9.6-SNAPSHOT" build.gradle.kts plugin examples web/package.json web/package-lock.json | grep -v node_modules` → 无残留。

- [ ] **Step 4: shadowJar + journal + commit + push（controller 验签）**

Run: `./gradlew :plugin:shadowJar --no-daemon` → 出 `HikariCanvas-0.9.7-SNAPSHOT.jar`。
`docs/journal.md` 顶部追加一条；commit（显式文件清单）+ push + `gh api .../commits/<sha> --jq '.commit.verification.verified'` == true。

---

## Self-Review

- **Spec coverage**：ScriptRuleValidator ~94（T2）+ checkConditionSyntax ~8（T3）= 全部校验报错纳入 lang yml + 按编辑器 locale 渲染；editorLocale 端到端（前端 auth → session → 渲染，T1）；ScriptOpDispatcher 的 Envelope.error 英文码不动（本就英文）。
- **Placeholder scan**：基建给了完整代码；T2/T3 的 ~100 站点是机械转换，给了 4 型示范 + key 命名约定 + "编译器标漏改 + LangFileParityTest 验对齐"兜底（非 TODO，是有界机械任务）。en 译文要求地道。
- **Type consistency**：`ValidationError` T1 定义、T2/T3 用；`Messages.plain` T1 定义、`renderValidation`（T2 加、T3 复用）调用；`Session.editorLocale` T1 定义、`renderValidation` 读；ScriptOpDispatcher 注入 `Messages`（T1）→ 使用（T2/T3）。
- **locale 正确性**：编辑器 UI 语言（前端 ui.locale → auth → session.editorLocale → 渲染），符合用户决策；缺失回退 Messages 默认 locale。
