# 0.7.1-P5 剩余动作（停止 / 粒子 / 等待直到）实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`).

**Goal:** 补齐 0.7.1 脚本动作的最后 3 个：停止本脚本（StopScript）、播放粒子（PlayParticle）、等待直到条件满足/超时（WaitUntil）。

**Architecture:** 后端为主——3 个 `Action` record + 序列化 + 校验 + 执行 + 权限。PlayParticle 照搬 PlaySound 的主线程 hop + 墙世界坐标。StopScript 在 ScriptRunner 内 `stack.clear()` 中止 run。WaitUntil **不阻塞 runner 线程**——首次计 1 action + 压栈后续 + 启动独立 `pollWaitUntil` 递归调度（满足/超时续接，否则 tick 后再评估），轮询不重入 action 循环故不重复计 Budget。前端高度复用：粒子=`select` 下拉、条件=`if` 的 ConditionBuilder、表单组件零改。

**Tech Stack:** Java 21（Paper 1.21.11，JUnit5 + MockBukkit）+ Vue3/TS（vitest）。

**契约:** `docs/scripting-0.7.1.md` §4 + §9（P5 决策已固化）。协议 v5 已是当前版本，新动作 type 是 v5 内扩展，**不升版本**。

**测试基线（开工前）:** 后端 1703 / 前端 1122。

---

## 文件结构

**后端**（`plugin/src/main/java/moe/hikari/canvas/script/`）:
- `Action.java` — +3 record + permits
- `ActionDeserializer.java` / `ActionSerializer.java` — +3 case
- `ScriptRuleValidator.java` — +3 case + 常数
- `engine/ActionExecutor.java` — +doPlayParticle（主线程 hop），StopScript/WaitUntil 返 error 防御
- `engine/ScriptRunner.java` — StopScript（clear stack）+ WaitUntil（pollWaitUntil 递归）
- `ScriptPermissions.java` — +3 case（PlayParticle→sound）

**前端**（`web/src/`）:
- `types/protocol.ts` — +3 union branch
- `script/model/blockDefs.ts` — PARTICLE_OPTIONS + 3 ACTION_DEFS + 3 makeDefaultAction case
- `script/model/validator.ts` — 常数 + 3 case
- `i18n/messages.ts` — blocks/fields/fieldOptions key（中英）

**粒子白名单（双端对齐，14 个）:** `minecraft:` + `flame, smoke, heart, happy_villager, crit, enchant, portal, firework, note, cloud, lava, dripping_water, end_rod, totem_of_undying`

---

# 后端

### Task 1: 3 个 Action record + permits + 序列化 + roundtrip 单测

**Files:**
- Modify: `Action.java` / `ActionDeserializer.java` / `ActionSerializer.java`
- Test: `ActionSerializationTest.java`（找现有序列化往返测试文件，追加；若无则新建）

- [ ] **Step 1: 写失败测试（序列化往返）**

在现有 Action 序列化测试里追加（参照现有 record 的 roundtrip 断言风格）：

```java
@Test
void stopScript_roundtrip() {
    Action a = new Action.StopScript();
    String json = serializer.serialize(a); // 用现有 ObjectMapper/序列化入口
    assertThat(json).contains("\"type\":\"stopScript\"");
    Action back = deserialize(json);
    assertThat(back).isEqualTo(a);
}

@Test
void playParticle_roundtrip() {
    Action a = new Action.PlayParticle("minecraft:flame", 10, 0.5, 0.5, 0.5);
    Action back = deserialize(serializer.serialize(a));
    assertThat(back).isEqualTo(a);
}

@Test
void waitUntil_roundtrip() {
    Action a = new Action.WaitUntil("var(\"user/x\") > 0", 5000L);
    Action back = deserialize(serializer.serialize(a));
    assertThat(back).isEqualTo(a);
}
```

- [ ] **Step 2: 跑测试看红**

Run: `./gradlew :plugin:test --tests "*ActionSerialization*"`（或对应类名）
Expected: FAIL — record 未定义。

- [ ] **Step 3a: Action.java 加 permits + 3 record**

permits 列表（约 L19）末尾加 `, Action.StopScript, Action.PlayParticle, Action.WaitUntil`。

record 区（照现有 record 风格，含 wireType）：

```java
/** 0.7.1-P5：中止当前脚本运行（清帧栈，后续动作不执行）。无字段。 */
record StopScript() implements Action {
    @Override public String wireType() { return "stopScript"; }
}

/** 0.7.1-P5：在墙世界坐标播放粒子。particleId=minecraft:xxx（白名单）；count 个；offset 为墙原点的偏移格。 */
record PlayParticle(String particleId, int count, double offsetX, double offsetY, double offsetZ) implements Action {
    @Override public String wireType() { return "playParticle"; }
}

/** 0.7.1-P5：轮询条件，满足或 timeoutMs 超时才继续。condition 复用 If 的条件文法。 */
record WaitUntil(String condition, long timeoutMs) implements Action {
    @Override public String wireType() { return "waitUntil"; }
}
```

- [ ] **Step 3b: ActionDeserializer 加 3 case**

在 `fromNode` 的 switch（约 L46）加（用现有 helper requireText/requireInt/requireDouble/requireLong）：

```java
case "stopScript" -> new Action.StopScript();
case "playParticle" -> new Action.PlayParticle(
        requireText(ctxt, node, "particleId", type),
        requireInt(ctxt, node, "count", type),
        requireDouble(ctxt, node, "offsetX", type),
        requireDouble(ctxt, node, "offsetY", type),
        requireDouble(ctxt, node, "offsetZ", type));
case "waitUntil" -> new Action.WaitUntil(
        requireText(ctxt, node, "condition", type),
        requireLong(ctxt, node, "timeoutMs", type));
```

- [ ] **Step 3c: ActionSerializer 加 3 case**

在 pattern match（约 L25）加（照现有 writeXxxField 风格）：

```java
case Action.StopScript a -> { /* 仅 type，已写 */ }
case Action.PlayParticle a -> {
    gen.writeStringField("particleId", a.particleId());
    gen.writeNumberField("count", a.count());
    gen.writeNumberField("offsetX", a.offsetX());
    gen.writeNumberField("offsetY", a.offsetY());
    gen.writeNumberField("offsetZ", a.offsetZ());
}
case Action.WaitUntil a -> {
    gen.writeStringField("condition", a.condition());
    gen.writeNumberField("timeoutMs", a.timeoutMs());
}
```

- [ ] **Step 4: 跑测试看绿**

Run: `./gradlew :plugin:test --tests "*ActionSerialization*"`
Expected: PASS。若编译报 sealed permits 未穷尽，确认 ActionSerializer/Deserializer/Validator/Executor/Permissions 的 switch 都加了新 case（后续 Task 会补，此处先让序列化通过——其余 switch 可临时 `default -> throw` 占位，但**更建议**先把 Task 1-6 的 switch case 都加上再统一编译）。

- [ ] **Step 5: Commit**

```bash
git add plugin/src/main/java/moe/hikari/canvas/script/Action.java plugin/src/main/java/moe/hikari/canvas/script/ActionDeserializer.java plugin/src/main/java/moe/hikari/canvas/script/ActionSerializer.java plugin/src/test/java/...
git commit -m "0.7.1-P5: StopScript/PlayParticle/WaitUntil 3 record + 序列化往返"
```

---

### Task 2: ScriptRuleValidator 3 case + 常数

**Files:**
- Modify: `ScriptRuleValidator.java`
- Test: `ScriptRuleValidatorTest.java`（追加）

- [ ] **Step 1: 写失败测试**

```java
@Test
void playParticle_rejects_unknown_particle() {
    var rule = ruleWith(new Action.PlayParticle("minecraft:nonexist", 10, 0, 0, 0));
    assertThat(validator.validate(rule)).isPresent();
}
@Test
void playParticle_rejects_count_out_of_range() {
    assertThat(validator.validate(ruleWith(new Action.PlayParticle("minecraft:flame", 0, 0, 0, 0)))).isPresent();
    assertThat(validator.validate(ruleWith(new Action.PlayParticle("minecraft:flame", 2000, 0, 0, 0)))).isPresent();
}
@Test
void playParticle_accepts_valid() {
    assertThat(validator.validate(ruleWith(new Action.PlayParticle("minecraft:flame", 10, 0.5, 0.5, 0.5)))).isEmpty();
}
@Test
void waitUntil_rejects_blank_condition_and_bad_timeout() {
    assertThat(validator.validate(ruleWith(new Action.WaitUntil("", 5000)))).isPresent();
    assertThat(validator.validate(ruleWith(new Action.WaitUntil("var(\"user/x\")>0", 10)))).isPresent(); // < MIN
    assertThat(validator.validate(ruleWith(new Action.WaitUntil("var(\"user/x\")>0", 999999)))).isPresent(); // > MAX
}
@Test
void waitUntil_accepts_valid() {
    assertThat(validator.validate(ruleWith(new Action.WaitUntil("var(\"user/x\")>0", 5000)))).isEmpty();
}
@Test
void stopScript_always_valid() {
    assertThat(validator.validate(ruleWith(new Action.StopScript()))).isEmpty();
}
```

> `ruleWith(Action)` 是测试 helper（包一条规则含该 action）；若现有测试用别的工厂名，沿用之。

- [ ] **Step 2: 跑测试看红**

Run: `./gradlew :plugin:test --tests "*ScriptRuleValidator*"`
Expected: FAIL。

- [ ] **Step 3: 加常数 + 白名单 + 3 case**

常数区（约 L72）加：

```java
private static final int PARTICLE_COUNT_MIN = 1;
private static final int PARTICLE_COUNT_MAX = 1000;
private static final long WAIT_UNTIL_TIMEOUT_MIN = 50L;
private static final long WAIT_UNTIL_TIMEOUT_MAX = 60_000L;
private static final java.util.Set<String> PARTICLE_WHITELIST = java.util.Set.of(
        "minecraft:flame", "minecraft:smoke", "minecraft:heart", "minecraft:happy_villager",
        "minecraft:crit", "minecraft:enchant", "minecraft:portal", "minecraft:firework",
        "minecraft:note", "minecraft:cloud", "minecraft:lava", "minecraft:dripping_water",
        "minecraft:end_rod", "minecraft:totem_of_undying");
```

validateAction 的 switch（约 L165）加：

```java
case Action.StopScript ignored -> Optional.empty();
case Action.PlayParticle a -> {
    if (a.particleId() == null || !PARTICLE_WHITELIST.contains(a.particleId())) {
        yield Optional.of("粒子种类不在允许范围: " + a.particleId());
    }
    if (!(a.count() >= PARTICLE_COUNT_MIN && a.count() <= PARTICLE_COUNT_MAX)) {
        yield Optional.of("粒子数量需在 " + PARTICLE_COUNT_MIN + ".." + PARTICLE_COUNT_MAX + " 之间");
    }
    if (!(Double.isFinite(a.offsetX()) && Double.isFinite(a.offsetY()) && Double.isFinite(a.offsetZ()))) {
        yield Optional.of("粒子偏移必须是有限数值");
    }
    yield Optional.empty();
}
case Action.WaitUntil a -> {
    if (blank(a.condition()) || a.condition().length() > CONDITION_MAX) {
        yield Optional.of("等待条件不能为空且最多 " + CONDITION_MAX + " 字符");
    }
    if (!(a.timeoutMs() >= WAIT_UNTIL_TIMEOUT_MIN && a.timeoutMs() <= WAIT_UNTIL_TIMEOUT_MAX)) {
        yield Optional.of("超时时长需在 " + WAIT_UNTIL_TIMEOUT_MIN + ".." + WAIT_UNTIL_TIMEOUT_MAX + " 毫秒之间");
    }
    yield Optional.empty();
}
```

> `blank(...)` / `CONDITION_MAX` 是 validator 现有 helper/常数。

- [ ] **Step 4: 跑测试看绿** → **Step 5: Commit**

Run: `./gradlew :plugin:test --tests "*ScriptRuleValidator*"` → PASS

```bash
git commit -am "0.7.1-P5: ScriptRuleValidator 3 case + 粒子白名单/范围常数"
```

---

### Task 3: ActionExecutor doPlayParticle（主线程 hop）+ StopScript/WaitUntil 防御

**Files:**
- Modify: `engine/ActionExecutor.java`
- Test: `ActionExecutorTest.java`（追加）

- [ ] **Step 1: 写失败测试**（plugin==null 测试路径直跑 work，不需真 Bukkit）

```java
@Test
void playParticle_unknownWall_returnsError() {
    // wallRepo 返回空 → error step
    var step = executor.execute("w-nope", "actions/0",
            new Action.PlayParticle("minecraft:flame", 10, 0, 0, 0));
    assertThat(step.result()).isEqualTo("error");
}
@Test
void stopScript_inExecutor_returnsError_handledByRunner() {
    // StopScript 应由 Runner 处理；Executor 收到返 error（同 Wait/If/Repeat 防御）
    var step = executor.execute("w-1", "actions/0", new Action.StopScript());
    assertThat(step.result()).isEqualTo("error");
}
@Test
void waitUntil_inExecutor_returnsError_handledByRunner() {
    var step = executor.execute("w-1", "actions/0", new Action.WaitUntil("x>0", 5000));
    assertThat(step.result()).isEqualTo("error");
}
```

> playParticle "成功路径"难在纯单测里验（要主线程 + 真 World）；核心覆盖 unknownWall/解析失败的 error 分支即可（同 PlaySound 测试策略）。真实喷粒子留实测闸。

- [ ] **Step 2: 跑测试看红** → Run: `./gradlew :plugin:test --tests "*ActionExecutor*"` → FAIL

- [ ] **Step 3a: execute() dispatch 加 3 case**

在 pattern match（约 L138）加：

```java
case Action.PlayParticle a -> doPlayParticle(wallId, blockId, a);
case Action.StopScript a -> TraceStep.error(blockId, "stopScript 应由 ScriptRunner 处理");
case Action.WaitUntil a -> TraceStep.error(blockId, "waitUntil 应由 ScriptRunner 处理");
```

- [ ] **Step 3b: doPlayParticle（照 doPlaySound 范式，主线程 hop + 墙坐标）**

```java
private TraceStep doPlayParticle(String wallId, String blockId, Action.PlayParticle a) {
    if (wallRepo == null) {
        return TraceStep.error(blockId, "playParticle 不可用（WallRepo 未装配）");
    }
    WallRepo.Wall wall = wallRepo.loadById(wallId).orElse(null);
    if (wall == null || wall.key() == null) {
        return TraceStep.error(blockId, "wall 不存在: " + wallId);
    }
    // 解析 particleId → Particle（Registry 只读，任意线程安全；同 PlaySound 的 Registry.SOUNDS 范式）
    org.bukkit.Particle particle;
    try {
        NamespacedKey key = NamespacedKey.fromString(a.particleId().toLowerCase(java.util.Locale.ROOT));
        particle = key == null ? null : org.bukkit.Registry.PARTICLE_TYPE.get(key);
    } catch (RuntimeException | NoClassDefFoundError | ExceptionInInitializerError e) {
        return TraceStep.error(blockId, "粒子解析失败: " + a.particleId());
    }
    if (particle == null) {
        return TraceStep.error(blockId, "粒子不存在: " + a.particleId());
    }
    WallRepo.Wall w = wall;
    org.bukkit.Particle p = particle;
    Runnable work = () -> {
        try {
            World world = Bukkit.getWorld(w.key().world());
            if (world == null) {
                log.warning("[脚本] playParticle 跳过：世界未加载 " + w.key().world());
                return;
            }
            Location origin = new Location(world,
                    w.key().originX() + a.offsetX(),
                    w.key().originY() + a.offsetY(),
                    w.key().originZ() + a.offsetZ());
            world.spawnParticle(p, origin, a.count());
        } catch (Throwable t) {
            log.log(Level.WARNING, "[脚本] playParticle 执行失败: " + t.getMessage(), t);
        }
    };
    if (plugin == null) {
        work.run();
    } else {
        Bukkit.getScheduler().runTask(plugin, work);
    }
    return TraceStep.ok(blockId, "action", "particle " + a.particleId() + " x" + a.count());
}
```

> `Registry.PARTICLE_TYPE` 若在目标 Paper API 不可用，fallback `org.bukkit.Particle.valueOf(key.getKey().toUpperCase())`（实施时验证哪个可用，二选一）。

- [ ] **Step 4: 跑测试看绿** → **Step 5: Commit**

```bash
git commit -am "0.7.1-P5: ActionExecutor doPlayParticle(主线程hop+墙坐标) + StopScript/WaitUntil 防御"
```

---

### Task 4: ScriptPermissions 3 case

**Files:** Modify `ScriptPermissions.java` / Test `ScriptPermissionsTest.java`

- [ ] **Step 1: 写失败测试**

```java
@Test
void playParticle_requires_sound_facet() {
    var facets = ScriptPermissions.requiredFacets(List.of(new Action.PlayParticle("minecraft:flame",10,0,0,0)));
    assertThat(facets).contains(ScriptPermissions.NODE_SOUND);
}
@Test
void stopScript_and_waitUntil_only_base_edit() {
    var f1 = ScriptPermissions.requiredFacets(List.of(new Action.StopScript()));
    var f2 = ScriptPermissions.requiredFacets(List.of(new Action.WaitUntil("x>0",5000)));
    assertThat(f1).doesNotContain(ScriptPermissions.NODE_SOUND, ScriptPermissions.NODE_COMMAND);
    assertThat(f2).doesNotContain(ScriptPermissions.NODE_SOUND, ScriptPermissions.NODE_COMMAND);
}
```

> 用现有 ScriptPermissions 的公开入口名（调研里是 scanActions；若公开方法叫别的，沿用之）。

- [ ] **Step 2: 看红** → **Step 3: scanActions switch 加 3 case**

```java
case Action.PlayParticle ignored -> facets.add(NODE_SOUND);
case Action.StopScript ignored -> { }
case Action.WaitUntil ignored -> { }
```

- [ ] **Step 4: 看绿** → **Step 5: Commit**

```bash
git commit -am "0.7.1-P5: ScriptPermissions PlayParticle→sound 面，Stop/WaitUntil 仅 edit"
```

---

### Task 5: ScriptRunner StopScript（清栈中止）

**Files:** Modify `engine/ScriptRunner.java` / Test `ScriptRunnerTest.java`

- [ ] **Step 1: 写失败测试**

```java
@Test
void stopScript_aborts_remaining_actions() {
    // 规则：log "a" → stopScript → log "b"。运行后 trace 含 a + stopScript，不含 b。
    var rule = ruleWithActions(
            new Action.Log("a"), new Action.StopScript(), new Action.Log("b"));
    var trace = runAndCollectTrace(rule); // 现有测试 helper：跑一次收 trace
    assertThat(traceDetails(trace)).anyMatch(s -> s.contains("a"));
    assertThat(traceDetails(trace)).noneMatch(s -> s.contains("\"b\"") || s.endsWith("b"));
    // run 正常结束（finish ok），非 blocked
}
```

> 用现有 ScriptRunnerTest 的运行 helper（构造 ScriptRunner + fake ActionSink + 触发一次 + 收 trace）。沿用其装配方式。

- [ ] **Step 2: 看红** → **Step 3: runFrames 加 StopScript 分支**

在 runFrames 的 action 处理区（wait 分支附近，约 L276 前），加：

```java
if (a instanceof Action.StopScript) {
    st.trace.add(TraceStep.ok(blockId, "action", "stopScript"));
    stack.clear();   // 清空帧栈 → while 外层自然退出 → finish(st, "ok")
    break;           // 跳出内层 while；外层 while 见栈空也退出
}
```

> 注意：内层 while 遍历当前帧的 actions、外层 while 遍历 stack。`stack.clear()` + `break`（跳内层）后，外层 `while (!stack.isEmpty())` 为假退出 → `finish(st,"ok")`。确认现有 runFrames 是双层 while（调研：行 234-336）；若是带 label 的单结构，用 `continue outer` 等价跳法。

- [ ] **Step 4: 看绿** → **Step 5: Commit**

```bash
git commit -am "0.7.1-P5: ScriptRunner StopScript 清栈中止 run"
```

---

### Task 6: ScriptRunner WaitUntil（独立 pollWaitUntil 递归，不阻塞/不重复计费）

**Files:** Modify `engine/ScriptRunner.java` / Test `ScriptRunnerTest.java`

> **本 Task 是 P5 难点。** 设计（§9 已固化）：WaitUntil 首次进入计 1 个 action（循环顶已 ++）→ 压栈后续（i+1）→ 启动独立 `pollWaitUntil` 递归。轮询走独立调度**不重入 action 循环**，故不重复计 actionCount，也不被 Budget 误拦。

- [ ] **Step 1: 写失败测试**（用可控的 fake scheduler + fake ConditionEvaluator）

```java
@Test
void waitUntil_satisfied_immediately_continues() {
    // condition 一进来就 true → 不挂起，直接执行后续 log "after"
    fakeConditions.setResult(true);
    var rule = ruleWithActions(new Action.WaitUntil("x>0", 5000), new Action.Log("after"));
    var trace = runAndCollectTrace(rule);
    assertThat(traceDetails(trace)).anyMatch(s -> s.contains("after"));
}

@Test
void waitUntil_polls_until_condition_true() {
    // 前 2 次 false，第 3 次 true。用 fake scheduler 手动 tick。
    fakeConditions.setResults(false, false, true);
    var rule = ruleWithActions(new Action.WaitUntil("x>0", 5000), new Action.Log("after"));
    startRun(rule);
    fakeScheduler.advanceOneTask(); // poll 1 (false → reschedule)
    fakeScheduler.advanceOneTask(); // poll 2 (false → reschedule)
    fakeScheduler.advanceOneTask(); // poll 3 (true → schedule continue)
    fakeScheduler.advanceOneTask(); // runFrames 续接 → log after
    assertThat(traceDetails(collectedTrace())).anyMatch(s -> s.contains("after"));
}

@Test
void waitUntil_timeout_continues_anyway() {
    fakeConditions.setResult(false); // 永不满足
    // timeoutMs 很小 + fake clock 推进过 deadline → 超时后继续
    var rule = ruleWithActions(new Action.WaitUntil("x>0", 100), new Action.Log("after"));
    startRun(rule);
    fakeClock.advance(150); // 过 deadline
    fakeScheduler.advanceOneTask(); // poll → timedOut → schedule continue
    fakeScheduler.advanceOneTask(); // 续接 → after
    assertThat(traceDetails(collectedTrace())).anyMatch(s -> s.contains("after"));
}

@Test
void waitUntil_polling_does_not_exhaust_budget() {
    // 长超时 + 多次轮询，actionCount 不应因轮询溢出（只首次计 1）
    fakeConditions.setResults(false, false, false, false, false, true);
    var rule = ruleWithActions(new Action.WaitUntil("x>0", 30000), new Action.Log("after"));
    startRun(rule);
    for (int i = 0; i < 6; i++) fakeScheduler.advanceOneTask();
    fakeScheduler.advanceOneTask();
    var trace = collectedTrace();
    assertThat(traceDetails(trace)).anyMatch(s -> s.contains("after")); // 没被 blocked
    assertThat(traceDetails(trace)).noneMatch(s -> s.contains("超上限") || s.contains("blocked"));
}
```

> 需要测试 seam：`fakeScheduler`（记录待执行 task，手动 advanceOneTask）+ `fakeConditions`（按序返回结果）+ `fakeClock`（System.currentTimeMillis 的注入 seam）。**实施前先看 ScriptRunner 现有测试怎么注入 scheduler / 时钟**——若现有 wait 测试已有 fake scheduler，沿用；若 currentTimeMillis 无注入 seam，加一个 `LongSupplier clock`（默认 System::currentTimeMillis，测试注入）。

- [ ] **Step 2: 跑测试看红**

Run: `./gradlew :plugin:test --tests "*ScriptRunner*"`
Expected: FAIL。

- [ ] **Step 3a: 加时钟 seam（若无）**

ScriptRunner 加字段 `private final java.util.function.LongSupplier clock;`（构造注入，生产传 `System::currentTimeMillis`）。所有 `System.currentTimeMillis()` 改 `clock.getAsLong()`。

- [ ] **Step 3b: runFrames 加 WaitUntil 分支**

在 wait 分支附近加（i 是当前下标、acts/f.prefix() 同 wait 范式）：

```java
if (a instanceof Action.WaitUntil wu) {
    st.trace.add(TraceStep.ok(blockId, "action", "waitUntil 开始"));
    long deadline = clock.getAsLong() + wu.timeoutMs();
    // 压栈后续动作（i+1）；当前 WaitUntil 已计 1 个 action（循环顶 ++）。
    stack.push(new Frame(acts, i + 1, f.prefix()));
    Deque<Frame> cont = new ArrayDeque<>(stack);
    pollWaitUntil(st, wu, deadline, cont, blockId);
    return; // 本段结束，等轮询满足/超时后续接
}
```

- [ ] **Step 3c: pollWaitUntil 私有方法（独立递归调度）**

```java
/**
 * 0.7.1-P5：WaitUntil 轮询——每 tick 评估 condition，满足 / 超时 → 续接后续动作（cont），
 * 否则 tick 后再评估。走独立调度，<b>不重入 runFrames 的 action 循环</b>，故轮询不重复计 actionCount、
 * 不被 Budget 误拦。shutdown / 调度拒绝 → 安静放弃（同 wait 续接的容错）。
 */
private void pollWaitUntil(RunState st, Action.WaitUntil wu, long deadline, Deque<Frame> cont, String blockId) {
    if (shutdown) return;
    boolean satisfied;
    try {
        satisfied = conditions.eval(wu.condition(), st.wallId);
    } catch (RuntimeException e) {
        satisfied = false; // 评估异常按未满足，靠超时兜底
    }
    boolean timedOut = clock.getAsLong() >= deadline;
    if (satisfied || timedOut) {
        st.trace.add(TraceStep.ok(blockId, "action", satisfied ? "waitUntil 满足" : "waitUntil 超时"));
        try {
            scheduler.schedule(() -> runFrames(st, cont), 0L); // 续接后续动作
        } catch (RejectedExecutionException ignored) { }
        return;
    }
    try {
        scheduler.schedule(() -> pollWaitUntil(st, wu, deadline, cont, blockId), WAIT_UNTIL_TICK_MS);
    } catch (RejectedExecutionException ignored) { }
}
```

加常数：`private static final long WAIT_UNTIL_TICK_MS = 100L;`

> **ActionExecutor dispatch 已对 WaitUntil 返 error（Task 3）**——但 WaitUntil 在 runFrames 被本分支拦截、`return` 不会走到 `sink.execute`，故防御 error 永不触发（同 Wait/If/Repeat）。确认 runFrames 里 WaitUntil 分支在"调 sink.execute 普通动作"之前。

- [ ] **Step 4: 跑测试看绿** → **Step 5: Commit**

```bash
git commit -am "0.7.1-P5: ScriptRunner WaitUntil(独立 pollWaitUntil 递归,不阻塞/不重复计费/超时兜底)"
```

---

# 前端

### Task 7: protocol + blockDefs + validator + 单测

**Files:**
- Modify: `types/protocol.ts` / `script/model/blockDefs.ts` / `script/model/validator.ts`
- Test: `script/model/__tests__/validator.test.ts`（追加，找现有 validator 测试文件）

- [ ] **Step 1: protocol.ts 加 3 union**（ScriptAction 尾部，约 L553）

```typescript
| { type: 'stopScript' }
| { type: 'playParticle'; particle: string; count: number; offsetX: number; offsetY: number; offsetZ: number }
| { type: 'waitUntil'; condition: string; timeoutMs: number }
```

- [ ] **Step 2: blockDefs.ts**

(a) 加 PARTICLE_OPTIONS 常量（选项区，约 L126，14 个与后端白名单对齐）：

```typescript
const PARTICLE_OPTIONS: FieldOption[] = [
    { value: 'minecraft:flame', labelKey: 'script.fieldOptions.particleFlame' },
    { value: 'minecraft:smoke', labelKey: 'script.fieldOptions.particleSmoke' },
    { value: 'minecraft:heart', labelKey: 'script.fieldOptions.particleHeart' },
    { value: 'minecraft:happy_villager', labelKey: 'script.fieldOptions.particleHappy' },
    { value: 'minecraft:crit', labelKey: 'script.fieldOptions.particleCrit' },
    { value: 'minecraft:enchant', labelKey: 'script.fieldOptions.particleEnchant' },
    { value: 'minecraft:portal', labelKey: 'script.fieldOptions.particlePortal' },
    { value: 'minecraft:firework', labelKey: 'script.fieldOptions.particleFirework' },
    { value: 'minecraft:note', labelKey: 'script.fieldOptions.particleNote' },
    { value: 'minecraft:cloud', labelKey: 'script.fieldOptions.particleCloud' },
    { value: 'minecraft:lava', labelKey: 'script.fieldOptions.particleLava' },
    { value: 'minecraft:dripping_water', labelKey: 'script.fieldOptions.particleDripWater' },
    { value: 'minecraft:end_rod', labelKey: 'script.fieldOptions.particleEndRod' },
    { value: 'minecraft:totem_of_undying', labelKey: 'script.fieldOptions.particleTotem' },
];
```

(b) ACTION_DEFS 加 3 条（约 L406；category='action'，颜色 CATEGORY_COLOR_VAR.action）：

```typescript
stopScript: {
    kind: 'stopScript', category: 'action', colorVar: CATEGORY_COLOR_VAR.action,
    labelKey: 'script.blocks.stopScript', fields: [],
},
playParticle: {
    kind: 'playParticle', category: 'action', colorVar: CATEGORY_COLOR_VAR.action,
    labelKey: 'script.blocks.playParticle', fields: [
        { name: 'particle', type: 'select', labelKey: 'script.fields.particle', options: PARTICLE_OPTIONS },
        { name: 'count', type: 'number', labelKey: 'script.fields.particleCount', min: 1, max: 1000, step: 1 },
        { name: 'offsetX', type: 'number', labelKey: 'script.fields.offsetX', step: 0.5 },
        { name: 'offsetY', type: 'number', labelKey: 'script.fields.offsetY', step: 0.5 },
        { name: 'offsetZ', type: 'number', labelKey: 'script.fields.offsetZ', step: 0.5 },
    ],
},
waitUntil: {
    kind: 'waitUntil', category: 'action', colorVar: CATEGORY_COLOR_VAR.action,
    labelKey: 'script.blocks.waitUntil', fields: [
        { name: 'condition', type: 'condition', labelKey: 'script.fields.condition' },
        { name: 'timeoutMs', type: 'number', labelKey: 'script.fields.timeoutMs', min: 50, max: 60000, step: 50 },
    ],
},
```

(c) makeDefaultAction 加 3 case（约 L627）：

```typescript
case 'stopScript': return { type: 'stopScript' };
case 'playParticle': return { type: 'playParticle', particle: 'minecraft:flame', count: 10, offsetX: 0, offsetY: 0, offsetZ: 0 };
case 'waitUntil': return { type: 'waitUntil', condition: 'var("user/score") > 0', timeoutMs: 5000 };
```

> 确认这 3 个 kind 也加进了 palette / 动作列表（若 blockDefs 有"可拖出的动作清单"数组，把 stopScript/playParticle/waitUntil 加进去，否则 palette 里看不到）。

- [ ] **Step 3: validator.ts 加常数 + 3 case + 测试**

常数（约 L87）：

```typescript
export const PARTICLE_COUNT_MIN = 1;
export const PARTICLE_COUNT_MAX = 1000;
export const WAITUNTIL_TIMEOUT_MIN = 50;
export const WAITUNTIL_TIMEOUT_MAX = 60000;
export const PARTICLES: ReadonlySet<string> = new Set([
    'minecraft:flame','minecraft:smoke','minecraft:heart','minecraft:happy_villager',
    'minecraft:crit','minecraft:enchant','minecraft:portal','minecraft:firework',
    'minecraft:note','minecraft:cloud','minecraft:lava','minecraft:dripping_water',
    'minecraft:end_rod','minecraft:totem_of_undying',
]);
```

validateAction switch（约 L436）：

```typescript
case 'stopScript': break;
case 'playParticle': {
    if (action.particle == null || !PARTICLES.has(action.particle)) {
        errors.push({ blockId: path, message: `粒子种类不在允许范围: ${action.particle}` });
    }
    if (typeof action.count !== 'number' || action.count < PARTICLE_COUNT_MIN || action.count > PARTICLE_COUNT_MAX) {
        errors.push({ blockId: path, message: `粒子数量需在 ${PARTICLE_COUNT_MIN}..${PARTICLE_COUNT_MAX} 之间` });
    }
    if (!Number.isFinite(action.offsetX) || !Number.isFinite(action.offsetY) || !Number.isFinite(action.offsetZ)) {
        errors.push({ blockId: path, message: '粒子偏移必须是有限数值' });
    }
    break;
}
case 'waitUntil': {
    if (isBlank(action.condition) || action.condition.length > CONDITION_MAX) {
        errors.push({ blockId: path, message: `等待条件不能为空且最多 ${CONDITION_MAX} 字符` });
    }
    if (typeof action.timeoutMs !== 'number' || action.timeoutMs < WAITUNTIL_TIMEOUT_MIN || action.timeoutMs > WAITUNTIL_TIMEOUT_MAX) {
        errors.push({ blockId: path, message: `超时时长需在 ${WAITUNTIL_TIMEOUT_MIN}..${WAITUNTIL_TIMEOUT_MAX} 毫秒之间` });
    }
    break;
}
```

测试（validator.test.ts 追加）：

```typescript
it('playParticle: 非白名单粒子 + count 越界报错', () => {
    expect(validateRule(ruleWith({ type: 'playParticle', particle: 'minecraft:nope', count: 10, offsetX: 0, offsetY: 0, offsetZ: 0 })).length).toBeGreaterThan(0);
    expect(validateRule(ruleWith({ type: 'playParticle', particle: 'minecraft:flame', count: 9999, offsetX: 0, offsetY: 0, offsetZ: 0 })).length).toBeGreaterThan(0);
    expect(validateRule(ruleWith({ type: 'playParticle', particle: 'minecraft:flame', count: 10, offsetX: 0, offsetY: 0, offsetZ: 0 }))).toEqual([]);
});
it('waitUntil: 空条件 / timeout 越界报错', () => {
    expect(validateRule(ruleWith({ type: 'waitUntil', condition: '', timeoutMs: 5000 })).length).toBeGreaterThan(0);
    expect(validateRule(ruleWith({ type: 'waitUntil', condition: 'var("user/x")>0', timeoutMs: 10 })).length).toBeGreaterThan(0);
    expect(validateRule(ruleWith({ type: 'waitUntil', condition: 'var("user/x")>0', timeoutMs: 5000 }))).toEqual([]);
});
it('stopScript: 永远合法', () => {
    expect(validateRule(ruleWith({ type: 'stopScript' }))).toEqual([]);
});
```

> `ruleWith(action)` 沿用 validator.test.ts 现有 helper（包一条规则）。

- [ ] **Step 4: build + vitest 看绿**

Run: `cd web && ./node_modules/.bin/vite build --clearScreen false && pkill -f vitest; ./node_modules/.bin/vitest run src/script/model/__tests__/validator.test.ts`
Expected: build 成功 + 测试 PASS。

- [ ] **Step 5: Commit**

```bash
git commit -am "0.7.1-P5 前端: 3 动作 protocol/blockDefs/validator + 粒子下拉 + 单测"
```

---

### Task 8: i18n 中英 + 全量测试 + 打包 + journal

**Files:** `i18n/messages.ts` / `docs/journal.md`

- [ ] **Step 1: messages.ts 加 key（中英两段对齐）**

zh `blocks`：`stopScript: '停止脚本', playParticle: '播放粒子', waitUntil: '等待条件',`
zh `fields`：`particle: '粒子种类', particleCount: '粒子数量', offsetX: '横向偏移', offsetY: '纵向偏移', offsetZ: '竖向偏移', timeoutMs: '超时（毫秒）',`
zh `fieldOptions`：14 个 `particleFlame: '火焰'` 等（火焰/烟雾/爱心/开心村民/暴击/附魔/传送门/烟花/音符/云/熔岩/水滴/末地烛/图腾）
en 对应段：`stopScript: 'Stop script', playParticle: 'Play particle', waitUntil: 'Wait until', ...`（particle: 'Particle', particleCount: 'Count', offsetX/Y/Z: 'Offset X/Y/Z', timeoutMs: 'Timeout (ms)'）+ 14 个粒子英文名（Flame/Smoke/Heart/Happy villager/Crit/Enchant/Portal/Firework/Note/Cloud/Lava/Dripping water/End rod/Totem）

> 确认 i18n 是强类型——漏 key build 会报错，全补齐中英两段。

- [ ] **Step 2: 后端全量 + 前端全量 + build**

```bash
./gradlew :plugin:test 2>&1 | tail -5
cd web && ./node_modules/.bin/vite build --clearScreen false && pkill -f vitest; ./node_modules/.bin/vitest run 2>&1 | grep -E "Test Files|Tests "
```
Expected: 后端 1703+新增 全绿；前端 1122+新增 全绿。

- [ ] **Step 3: 打包**

```bash
cd web && ./node_modules/.bin/vite build --clearScreen false
cd .. && ./gradlew :plugin:shadowJar 2>&1 | tail -3
```

- [ ] **Step 4: journal + commit + push**

`docs/journal.md` 顶部加 0.7.1-P5 完工条目（日期 2026-06-13 · 范围 · 改动 · 关联文件）。

```bash
git add -A && git commit -m "0.7.1-P5 完工: 停止/粒子/等待直到 3 动作 + i18n + journal" && git push origin main
gh api /repos/HyacinthHaru/HikariCanvas/commits/$(git rev-parse HEAD) --jq '.commit.verification.verified'
```

---

## P5 完整实测闸（打包后用户验）

1. **停止脚本**：规则里放 `log "A" → 停止脚本 → log "B"` → 触发 → 只看到 A 的效果，B 不执行
2. **播放粒子**：规则放「播放粒子 火焰 ×10」→ 触发 → 墙周喷出火焰粒子；换几种粒子（爱心/烟花）验证下拉
3. **等待直到**：规则放「等待直到 `var("user/x") > 0`（超时 5000）→ 改个元素颜色」→ 触发后元素不变 → `/canvas var set user/x 1`（或脚本里先 setVariable）→ 条件满足后元素变色；另验超时路径（条件永不满足 → 5 秒后仍继续）
4. **不卡服**：等待直到轮询期间服务器不卡（独立调度不阻塞主线程 / runner 线程）
5. **Budget 不误杀**：长超时（30s）的等待直到，轮询多次不触发"动作超上限"熔断

---

## Self-Review

- **Spec 覆盖**：§4 三动作（停止 Task5 / 粒子 Task3 / 等待直到 Task6）✅；§9 决策——粒子权限复用 sound（Task4）✅、WaitUntil 独立 poll 不重复计费（Task6）✅、StopScript clear stack（Task5）✅、粒子白名单双端对齐（Task2/3/7）✅。
- **类型一致**：record 字段（particleId/count/offsetX/Y/Z、condition/timeoutMs）后端 ↔ 前端 union（particle/count/.../condition/timeoutMs）命名注意——**后端 `particleId` vs 前端 `particle`**：wire 字段名必须一致！统一用 **`particle`**（改 Task1 record 字段名 particleId→particle + Deserializer/Serializer 字段名 + doPlayParticle 的 a.particle()）。⚠️ 实施时以 `particle` 为准，保证 wire 双端同名。
- **协议**：v5 不变（新 type 是 v5 内扩展）。
- **难点**：WaitUntil 时钟 seam + fake scheduler 测试——实施前先读 ScriptRunner 现有 wait 测试的调度注入方式，沿用。
- **风险**：`Registry.PARTICLE_TYPE` API 可用性（Task3 有 fallback）；spawnParticle 在 1.21 的签名（count 版足够）。
