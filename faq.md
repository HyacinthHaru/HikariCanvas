# 常见问题

遇到问题先在这里找。每条都直接给原因和做法。

## 编辑器链接点了打不开 / 没反应

链接是临时的，有三个限制：

- **一次性**：用过一次就失效。
- **15 分钟过期**：超时就作废。
- **只对本人有效**：别人点你的链接打不开，也别把链接发给别人。

解决办法：回游戏里输入 `/canvas open <编号或别名>`（或 `/canvas edit` 框新墙），拿一条新链接。

## 提示「身份验证失败」/「令牌已失效」

就是上面那条链接过期或用过了。回游戏重新 `/canvas open` 或 `/canvas edit` 拿新链接即可。

## 提示版本不兼容 / 一直连不上

通常是浏览器里打开的编辑器页面和服务器上的插件版本对不上。

- 先**刷新浏览器**（按 F5），多数情况能解决。
- 还不行就**清一下浏览器缓存**再打开链接。
- 如果服务器刚更新过插件，确认你用的是更新后重新生成的链接。

页面会自动重连几次；要是一直连不上，刷新页面或回游戏重新 `/canvas edit`。

## 在自己电脑能开，别人/远程打不开

插件默认只允许**本机**访问，所以远程或公网点链接会连不上。

要让其他人也能访问，需要服主做反向代理配置。见 [安全与公网部署](/admin/security)。

## 编辑器里改了，游戏里没变

逐项排查：

- **确认在看同一面墙**。一个人可能有多面招牌，别盯错了墙。
- 改动是**自动保存 + 实时投影**的，正常情况下回游戏几乎立刻就能看到，不用点保存。
- 偶尔卡住不同步，**重开一次编辑器**（重新 `/canvas open`）通常就好了。

## 招牌空白 / 招牌不见了

可能的原因：

- **画框被破坏了**。招牌挂在墙上的展示框（画框）被打掉，画面就会显示异常。让管理员检查那面墙。
- **招牌被删了**。作者或管理员用 `/canvas delete` 删过，招牌就没了。
- **被锁定的招牌不会消失**，它只是变成只读，画面照常显示。

## 招牌锁定了，改不动

招牌被它的作者锁成**只读**了，这是防误改的功能。

只有**作者本人**能解锁：在编辑器顶部点那个**锁**按钮即可。其他人无法解锁。

## 字显示成方框 / 有些字缺失

你选的字体里没有这个字。最常见的是**用西文装饰字体写中文**——那些花体字根本不含汉字。

换一个支持你要写的语言的字体（比如中文用黑体、宋体一类）。详见 [文字与字体](/guide/text)。

## 颜色看着有点不对 / 渐变有色阶

游戏地图能用的颜色数量有限，画面里的颜色会**就近取最接近的那个**，所以和编辑器里看到的会略有差别，这是正常的。

渐变或半透明的地方如果出现一格一格的色阶，打开**柔和过渡**会顺滑很多。见 [填充、颜色与透明](/guide/fill)。

## 命令用不了 / 提示没权限

对应功能需要权限，你账号上没开。找服主开通即可。各功能对应的权限见 [权限](/admin/permissions)。
