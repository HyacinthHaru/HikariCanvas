name: DemoTrainPlugin
version: '0.9.13-SNAPSHOT'
main: ac.haru.hikaricanvas.demos.train.DemoTrainPlugin
api-version: '1.21'
description: HikariCanvas Push API demo — simulated train schedule with live ETA pushes.
authors:
  - HaruHyacinth

dependencies:
  server:
    HikariCanvas:
      load: BEFORE
      required: true
      join-classpath: true
