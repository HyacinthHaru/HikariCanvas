# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon
._*

# JetBrains / IntelliJ
.idea/
*.iml
*.ipr
*.iws
out/

# VS Code
.vscode/
*.code-workspace

# Claude Code
.claude/

# Eclipse
.project
.classpath
.settings/
bin/

# Gradle
.gradle/
build/
!**/src/**/build/
!gradle-wrapper.jar

# Maven (若后续使用)
target/
pom.xml.tag
pom.xml.releaseBackup
pom.xml.versionsBackup
pom.xml.next
release.properties
dependency-reduced-pom.xml

# Java
*.class
*.jar
*.war
*.ear
!gradle/wrapper/gradle-wrapper.jar
hs_err_pid*.log
replay_pid*.log

# Node / 前端
node_modules/
dist/
dist-ssr/
.vite/
.cache/
*.local
.env

# M5-C1 前端字体：由 ./gradlew downloadFonts 从 plugin/build/downloaded-fonts/ 同步进来
web/public/fonts/
.env.*
!.env.example

# TypeScript
*.tsbuildinfo

# 前端日志
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# 前端构建产物（最终拷贝进插件 jar 的目标位置由 gradle 任务控制，开发中不提交）
plugin/src/main/resources/web/

# 插件运行时数据（绝对不提交）
plugins/
logs/
world/
world_nether/
world_the_end/
*.db
*.db-journal
crash-reports/
cache/
*.log

# 本地测试服
test-server/
run/

# 密钥 / 凭据
*.keystore
*.jks
*.pem
*.key
*.crt
*.env.local

# 临时
*.tmp
*.tmp.*
*.swp
*.bak
*~

# 工具产生的本地台账 / 草稿，不入库
.superpowers/

# 云同步（iCloud / Dropbox 等）产生的冲突副本：`foo 2.ttf` / `Bar 2.java`。
# 2026-07-27 实测：同步服务往 plugin/build/ 里塞了 46 个这种副本（39 个是字体），
# 把 shadowJar 从 85MB 撑到 195MB；还把 0.9.9 改包名时删掉的 moe/ 旧包目录
# （361 个文件）和 0.9.16 退役的 7 个 YAML 模板整批恢复回工作区。
# **建议把本项目目录移出同步范围**，至少排除 build/ 与 node_modules/。
*[0-9] [0-9].*
* 2.*
* 3.*

# 渲染测试输出（CI 产出保留 expected，actual 不提交）
rendering-test/actual-java/
rendering-test/actual-web/
rendering-test/diff/

# Python（rendering-test 一致性向量生成器的编译缓存）
__pycache__/
*.pyc
